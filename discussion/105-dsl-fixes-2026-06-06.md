# 105 — Required DSL/EDN parsing fixes

## DSL

1. `parse_noncovalent_bond` only accepts `*`.
2. :a, :b are used in bonds, noncovalent bond syntax, not intuitive.

  {:id :b1
   :atoms [:c1 :c2]
   :type :double}

  {:id :hb1
   :atoms [:oxygen :hydrogen]
   :type "Hbd"}

3. Figure out ref / var scope, notation for cross-object ref /var accesses.
4. Need to specify the rules for ligand ordering in #C / #T in spec.
5. Check if entity constraints are correctly combined with string-based DSL. Which ones have higher priority in case of conflict?
6. Additional shorthands for rings, chains, fragments, compare to the graph traversal semantics.
7. Syntactic sugar for constraints (ex: :free-rotation, :berry-pseudorotation, :ring-flip for fluxionality constraints on stereoelements)?
8. Ensure that any permutation of keys in top-level EDN map is valid.
9. Break up long modules.

## AST

1. Review enum variants for ValueAst and other data-carrying AST types.
2. Variable scoping and cross checking
3. JointDomain
4. Haptic/coordination vs two-atom dative bonds
5. IUPAC stereo chemistry types from doc 106.
6. Consider separating directed and undirected non-covalent bonds
7. Reason through missing entity types
8. Break up MoleculeAst methods into multiple submodules by purpose
9. Check the shape of entity views for public fields, consider replacing by inline accessors.
   Check which accessors can be inlined.
10. Remove iterators over mutable accessors (atoms_mut etc), replace with for_each_<entity> taking closure, blocks
    entity interning. Verify if individual entity mutable accessors are ok (should be -- in-place mutation, re-interning on Drop).
11. Adjust imports (super:: within ast / dsl subtree, crate:: otherwise)
12. Update the ring membership derived predicates to match the constraints.
13. Break up long modules.
14. Make rings() -> RingsView() accessor on MoleculeAst, chang ring shared accessors to sit on RingsView::shared(id1: RingId, id2: RingId)
    instead of taking another ring view as argument. Sweep views as arguments again (including closure arguments), this should not happen.
15. Add cheap overrides for is_compatible instead of meet().is_some().

### Other

1. Check bond atoms accessors in TableIR Bond, should keep atom pair open as it is currently?
2. Review module sealing policies. Domain-specific modules (graph-core, perm, ast, msym, geometric-core) should only allow top-level
   exports).
3. ast crate has separate top-level imports from ast/ and dsl/: consider splitting out into umol-dsl.
4. Review pub(super) and pub(crate) visibility markers, restructure where possible to either have pub or private visibility.
5. Check resolvers and validators: Which need to be added?
6. Review error infrastructure.
7. Replace nom -> winnow for MOL/SDF parsing.
8. Add consistency checks to aromaticity validator (some atoms are in aromatic systems), stereo (inconsistent kinds). 
9. Add consistency check for ring count for all rings >= ring counts for individual ring sizes to validators.
10. Review other validators.
11. aromatic_increment -> aromatic_covalence
12. Check if sorting atoms in aromatic system/ multicenter bond storage messes up the relation to the electron counts.
13. Python API.
14. Add overlay inclusion configuration (dative, multicenter, noncovalent) to ring detection.
15. Make it easier to dump intermediate representations (TableIR, resolution results).
16. Consider adding Ground<T>(pub inner: T) that eliminates as_lit calls.

## Tooling

1. Span reporting and validation

```rust
struct Span {
    start: usize,
    end: usize,
}

struct Spanned<T> {
    value: T,
    span: Span,
}

enum SpannedEdnKind<'a> {
    Nil,
    Bool(bool),
    Int(i64),
    Str(Cow<'a, str>),
    Keyword(EdnKeyword<'a>),
    Vector(Vec<SpannedEdn<'a>>),
    Map(Vec<(SpannedEdn<'a>, SpannedEdn<'a>)>),
    // ...
}

type SpannedEdn<'a> = Spanned<SpannedEdnKind<'a>>;
}
```

Separate parsing infra + into_edn() / as_edn()

source text
    ├── parse_spanned() -> SpannedEdn     editor diagnostics
    └── read_string()   -> Edn            existing runtime API

2. Schema checking

EDN does not have a strong ecosystem for schema validation comparable to TOML or JSON. [Malli](https://github.com/metosin/malli) is closest.
