# Molecule operations worklist

Status: In Progress
Date: 2026-07-27
Relates: [086](086-molecule-ast-api-2026-04-16.md),
[096](096-valence-resolution-plan-2026-05-21.md),
[118](118-validator-architecture-2026-06-20.md),
[177](177-nomenclature-guide-2026-07-31.md),
[195](195-molecule-constraint-matching-2026-08-12.md),
[211](211-relation-frames-and-api-2026-08-26.md),
[213](213-editor-overlay-storage-2026-08-27.md),
[226](226-smiles-roundtrip-design-2026-09-10.md),
[227](227-repository-structure-hygiene-2026-09-10.md)

## Scope

This document tracks two additive operations over `Molecule`: conversion between
implicit and explicit hydrogen representations, and validation of the concrete
domains of inherent entity fields. It carries forward the still-relevant parts
of Phase 11a and Phase 12 from doc 086 using the current model.

It also tracks the deferred counts-resolver shell-capacity correction below.

The operation boundaries are:

- resolution fills undetermined state using a chemistry model;
- transformation rewrites one resolved representation into another;
- validation checks integrity, model-independent invariants, and
  model-dependent conformance without mutation.

The boundaries and validator composition established by docs 096 and 118 remain
in force. Correspondence-returning hydrogen variants belong to the separate
mutation-witness work. New or revised molecule-level constraint validation,
including unpaired-electron coupling constraints, is deferred to doc 195.

`ConnectivityModel` is already part of `ChemistryModel`, and
`ConnectivityConformanceValidator` already runs first in conformance
validation. Constraint invariants, aromaticity conformance, spin invariants,
overlay uniqueness, and aromatic/multicenter electron-vector shape are also
implemented. This document does not retain parallel work items for them.

## Implicit and explicit hydrogen transformations

Add two high-level transformer types under `umol-graph/src/ops/transform`, each
implementing `Transformer`. They are the ordinary public operation surface and
need no configuration:

- `HydrogenFolder` folds eligible explicit hydrogen atoms and their bonds into
  implicit-hydrogen counts;
- `HydrogenUnfolder` unfolds literal implicit-hydrogen counts into explicit
  hydrogen atoms and bonds.

The complete rewrite of topology, entity ids, stereo ligand frames, and stored
references belongs to graph IR. Expose the narrowly required low-level
`fold_explicit_hydrogens` and `unfold_implicit_hydrogens` functions from
`umol-graph-ir`; the `umol-graph` transformers delegate to them and own the
high-level operation contract. Their operation errors are
`FoldHydrogensError` and `UnfoldHydrogensError`. Do not duplicate the rewrite
in the chemistry layer or replace the transformers with inherent `Molecule`
methods.

### Public API shape

`umol-graph-ir` exposes the representation-owning operations from `ir`:

```rust
pub fn fold_explicit_hydrogens(
    molecule: &Molecule,
) -> Result<Molecule, FoldHydrogensError>;

pub fn unfold_implicit_hydrogens(
    molecule: &Molecule,
) -> Result<Molecule, UnfoldHydrogensError>;

pub enum FoldHydrogensError {
    NegativeImplicitHydrogenCount {
        atom: AtomId,
        count: i64,
    },
    StereoHydrogenConflict {
        entity: Entity,
        hydrogen: AtomId,
    },
}

pub enum UnfoldHydrogensError {
    NegativeImplicitHydrogenCount {
        atom: AtomId,
        count: i64,
    },
    NonConcreteStereoHydrogenCount {
        entity: Entity,
        atom: AtomId,
        value: NumForm,
    },
    StereoHydrogenCountMismatch {
        entity: Entity,
        atom: AtomId,
        implicit_hydrogens: i64,
        virtual_hydrogens: usize,
    },
}
```

`umol-graph` exposes `HydrogenFolder` and `HydrogenUnfolder` from
`ops::transform`. They are public stateless unit structs, implement
`Transformer`, and use the corresponding graph-IR error as `Transformer::Error`.
The graph crate re-exports those errors from `ops::transform`, so ordinary
callers do not need to import the lower-level crate merely to name the result.
Neither transformer has a configuration type or an inherent `Molecule` method.

The two standalone tier-2 validators are exposed from
`umol_graph::ops::validate` with this shape:

```rust
pub struct EntityFieldInvariantsValidator;

pub enum EntityFieldInvariantsContradiction {
    AtomChargeOutOfRange { atom: AtomId, value: i64 },
    AtomImplicitHydrogensOutOfRange { atom: AtomId, value: i64 },
    AtomLonePairsOutOfRange { atom: AtomId, value: i64 },
    BondOrderOutOfRange { bond: BondId, value: i64 },
    BondChargeOutOfRange { bond: BondId, value: i64 },
    DativeBondOrderOutOfRange { bond: DativeBondId, value: i64 },
    AromaticSystemChargeOutOfRange {
        system: AromaticSystemId,
        value: i64,
    },
    MulticenterBondChargeOutOfRange {
        bond: MulticenterBondId,
        value: i64,
    },
}

pub enum EntityFieldInvariantsError {}

impl EntityFieldInvariantsValidator {
    pub fn validate(
        &self,
        molecule: &Molecule,
    ) -> Result<
        Solution<(), EntityFieldInvariantsContradiction>,
        EntityFieldInvariantsError,
    >;
}

pub struct StereoLigandInvariantsValidator;

pub enum StereoLigandInvariantsContradiction {
    MissingActualLigands {
        entity: Entity,
        bearer: AtomId,
        ligands: Vec<AtomId>,
    },
    ImplicitHydrogenCountMismatch {
        entity: Entity,
        bearer: AtomId,
        implicit_hydrogens: i64,
        virtual_hydrogens: usize,
    },
    LonePairCountMismatch {
        entity: Entity,
        bearer: AtomId,
        lone_pairs: i64,
        virtual_lone_pairs: usize,
    },
}

pub enum StereoLigandInvariantsError {}

impl StereoLigandInvariantsValidator {
    pub fn validate(
        &self,
        molecule: &Molecule,
    ) -> Result<
        Solution<(), StereoLigandInvariantsContradiction>,
        StereoLigandInvariantsError,
    >;
}
```

Both validators are stateless unit structs with no configuration or
constructor method. They are not added to the public composite `Validator` in
this release.

The public type names, signatures, error and contradiction variants, and their
payloads above are settled. The validator error types are uninhabited but
retained for the uniform validator contract. Exact diagnostic display text and
private source-file placement are implementation details.

### API contract

- The operation objects are stateless engines, not carriers of validated state.
  Their input and output are ordinary closed `Molecule` values.
- The graph-IR functions return fresh molecules. `transform` delegates to the
  corresponding function, while `transform_into` computes a complete candidate
  and replaces its argument only on success. An error therefore leaves every
  caller-visible input unchanged.
- The first folding or unfolding operation that depends on a nonnegative
  literal implicit-hydrogen count or a consistent stereo ligand census checks
  that precondition itself. A prior validator call is not required.
- The validators inspect model-independent semantic predicates. They neither
  construct a stronger molecule type nor transform, repair, or resolve their
  input. A relevant non-literal field produces `Solution::Underdetermined`; a
  literal violation produces `Solution::Contradictory`.
- `generate_all` follows the existing deterministic-transformer contract: it
  yields the one transformed molecule on success and no item on error. Callers
  that require the typed diagnostic use `transform` or `transform_into`.
- Both transformations preserve `Molecule` representation integrity and are
  idempotent. No inverse, lossless, or general roundtrip law is exposed.
- This work adds no conversions, prepared or validated wrappers,
  correspondence-returning variants, or Python bindings.

### Expansion to explicit hydrogens

- A literal implicit-hydrogen count must be nonnegative; return
  `UnfoldHydrogensError` atomically for a negative count. For `n > 0`, add `n`
  hydrogen atoms and `n` localized single bonds; zero is unchanged.
- Each added hydrogen has the concrete form
  `H#i=#c0#h0#n0#u0#s`.
- Set the source atom's implicit-hydrogen field to literal zero.
- Leave undetermined or non-literal counts unchanged.
- Leave already explicit hydrogens unchanged; mixed explicit and implicit
  hydrogen representations therefore become fully explicit wherever the
  implicit count is literal.
- A stereo-atom frame and each stereo-bond endpoint contain a complete ligand
  census, not a selected subset. For every affected frame or endpoint, its
  number of virtual implicit-hydrogen ligands must equal the bearer's literal
  implicit-hydrogen count. A stereo entity therefore cannot exist on a bearer
  with more than one implicit hydrogen: the complete frame would require
  repeated implicit-hydrogen ligands, which representation integrity
  prohibits.
- Create explicit hydrogens once per bearer atom. Replace every virtual
  implicit-hydrogen occurrence anchored to that bearer with the corresponding
  newly explicit hydrogen. An atom with a literal count greater than one can
  still be unfolded when it is not the bearer of a stereo entity.
- Return `UnfoldHydrogensError` when an affected stereo frame or endpoint
  disagrees with a literal implicit-hydrogen count, or when a virtual ligand is
  backed by a non-literal count. An atom with a non-literal count and no such
  virtual ligand remains unchanged.
- Reapplying the transformer is a no-op.

### Collapse to implicit hydrogens

Collapse a hydrogen only when it is an unambiguous ordinary explicit hydrogen:

- exactly one incident localized bond;
- the bond has the concrete form `1#c0#u0#s` and no bond constraints;
- non-hydrogen partner;
- no isotope label, charge, spin, or other non-default atom state;
- no atom constraints;
- neither the hydrogen nor its bond is referenced by a molecule constraint;
- no participation in dative, aromatic, multicenter, or noncovalent relations.

The partner's implicit-hydrogen count must be a nonnegative literal. An
undetermined count does not authorize deleting an explicit hydrogen; leave
that hydrogen unchanged. A negative literal produces `FoldHydrogensError`
atomically. Otherwise remove the hydrogen and bond and increment the partner's
implicit-hydrogen count. No separate checked-arithmetic or overflow error is
part of this operation's contract.

An explicit hydrogen used as a stereo-atom or stereo-bond ligand is replaced
in place by an implicit-hydrogen ligand on its non-hydrogen partner. If the
complete affected stereo frames cannot represent the result without repeating
an implicit-hydrogen ligand, return `FoldHydrogensError` atomically. This
includes a pre-existing implicit hydrogen and multiple explicit hydrogen
ligands that would be folded onto the same stereo bearer. The transformer does
not remove the stereo entity to make the fold possible.

Preserve D/T, hydrides/protons, radicals, H2, other relation participants, and
any hydrogen whose information could not be represented after collapse. Mixed
explicit and literal implicit hydrogens are combined in the implicit count
when no stereo entity prevents it. Reapplying the transformer is a no-op.

No general losslessness or inverse roundtrip law is claimed. Folding removes
atom and bond identities, and mixed or non-foldable explicit representations
preclude a universal inverse law. Each operation returns only the transformed
`Molecule`; the correspondence or compaction that witnesses the change is
separate mutation-witness work.

Verification must cover per-direction behavior, idempotence, field preservation,
relation preservation, representative reconstructible cases, and every error
boundary. Do not state a general roundtrip property.

## Stereo ligand mutation

The participant-mutation prerequisite is complete as of 2026-09-17 (S0–S4 below).
S5 adds the approved whole-relation addition/removal surface to all five sets.
The graph-IR/editor mutation surface and its delegation to storage still need
design. Hydrogen transformation implementation remains separate follow-up work.

Both hydrogen transformations require a position-preserving substitution in a
stereo frame: unfolding replaces a virtual implicit-hydrogen ligand with the
new explicit hydrogen atom, while folding makes the inverse representation
change. The ligand occupying the position changes, but the position itself and
the configuration read against the frame do not.

This is distinct from both existing frame operations. `FrameTransport` applies
a supplied frame action, while `Reframe` selects the canonical representative
frame. Both preserve the ligand multiset, transport the frame-relative form,
and reorder the stored ligands. At the start of this work, that storage operation
was named `FixedVarBirelationSet::permute_2_with`; S3a applies the approved rename.
Ligand substitution preserves the frame position but changes the participant
multiset. It may therefore change the derived incidence index: replacing a
virtual ligand anchored to one atom with an actual ligand naming a newly added
hydrogen changes which atom ids the stereo entity references.

The immutable graph-IR readers remain
`StereoAtoms::ligands(id) -> &[StereoLigand]` and
`StereoBonds::ligands(id) -> &[StereoLigand]`. Their storage is backed by the
second factor of `FixedVarBirelationSet`. Before S2, its participants were immutable
except for multiset-preserving permutation. `MoleculeEditor` still
materializes a touched stereo family into owned entries and rebuilds indexed
relation storage when the molecule is published.

## Relation storage mutation

### Storage-owned mutation decision — 2026-09-15

Participant mutation belongs to relation storage. The editor must delegate to
that capability rather than use materialized entries as a substitute for it.
The prerequisite covers all five relation-set shapes: FixedRelationSet,
VarRelationSet, FixedFixedBirelationSet, FixedVarBirelationSet, and
VarVarBirelationSet. The earlier proposal to expose mutable ligand references
from the editor is withdrawn; a bare mutable reference cannot give indexed
storage control over completion of the replacement.

Design whole-factor replacement, single-participant replacement, insertion,
and removal together. Whole-factor replacement is the foundation: fixed
factors retain their arity, variable factors accept a new sequence, and
birelations can replace either factor or both together. Implement the complete
method family initially through whole-factor replacement; specialize later
only if justified.

Replacement preserves the relation id, payload, and supplied participant order.
It updates incidence over the complete relation, including both factors of a
birelation. Incidence is a union of references: replacing one participant must
retain an old node or edge incidence if another participant still references
it. This matters when a virtual stereo ligand and its site share an atom
reference. Graph core neither interprets the payload nor enforces molecular
semantics. Graph IR owns the coordinated topology, ligand, and payload changes
and the molecule publication boundary.

A single helper that scans and rewrites both stereo-atom and stereo-bond
frames is not the intended graph-IR abstraction. The entity families remain
explicit. Hydrogen substitution preserves positions and their configuration;
arbitrary participant replacement makes no such semantic promise. Folding must
substitute hydrogen ligands before topology removal, whose existing cascade
would otherwise remove stereo entities still referencing those hydrogens.

### Approved graph-core additions — 2026-09-15

The following methods are approved public additions in graph core. Every
method takes &mut self followed by id: RelationId; the tables list the
remaining arguments. All return (). An invalid relation id panics, consistently
with existing indexed access. An invalid factor-local position also panics:
replacement/removal require position < length; insertion permits position <=
length.

Participant values follow the same admission contract as new. NodeId and
EdgeId references inside participants are stored and indexed through
RelationParticipant::refs without checking membership in an external graph.
The relation set has no supplied graph or declared node/edge domain against
which to validate them. Repeated participant values remain representable;
incidence deduplicates references, not the stored participant sequence. Fixed
factor lengths are enforced by array types; variable factors permit any length,
including zero. There is no new Result, error type, or checked variant.

#### Whole-factor replacement

| Relation set | Method | Replacement arguments |
| --- | --- | --- |
| FixedRelationSet<P, D, N> | replace_participants | participants: [P; N] |
| VarRelationSet<P, D> | replace_participants | participants: &[P] |
| FixedFixedBirelationSet<L1, N1, L2, N2, D> | replace_participants | participants_1: [L1; N1], participants_2: [L2; N2] |
| FixedFixedBirelationSet<L1, N1, L2, N2, D> | replace_participants_1 | participants: [L1; N1] |
| FixedFixedBirelationSet<L1, N1, L2, N2, D> | replace_participants_2 | participants: [L2; N2] |
| FixedVarBirelationSet<L1, N1, L2, D> | replace_participants | participants_1: [L1; N1], participants_2: &[L2] |
| FixedVarBirelationSet<L1, N1, L2, D> | replace_participants_1 | participants: [L1; N1] |
| FixedVarBirelationSet<L1, N1, L2, D> | replace_participants_2 | participants: &[L2] |
| VarVarBirelationSet<L1, L2, D> | replace_participants | participants_1: &[L1], participants_2: &[L2] |
| VarVarBirelationSet<L1, L2, D> | replace_participants_1 | participants: &[L1] |
| VarVarBirelationSet<L1, L2, D> | replace_participants_2 | participants: &[L2] |

Arrays enforce fixed arity. Variable-factor slices may have any length,
including zero; storage copies them into its owned participant buffer. A
factor-specific replacement preserves the other factor. Replacing both factors
establishes incidence from their final union in one operation.

#### Single-participant operations

All of these operations are included in the initial implementation. They
construct the replacement factor and delegate to whole-factor replacement.

| Available on | Methods | Arguments after id |
| --- | --- | --- |
| FixedRelationSet, VarRelationSet | replace_participant | position: ParticipantPosition, participant: P |
| All three birelation sets | replace_participant_1 | position: ParticipantPosition, participant: L1 |
| All three birelation sets | replace_participant_2 | position: ParticipantPosition, participant: L2 |
| VarRelationSet | insert_participant | position: ParticipantPosition, participant: P |
| VarRelationSet | remove_participant | position: ParticipantPosition |
| FixedVarBirelationSet, VarVarBirelationSet | insert_participant_2 | position: ParticipantPosition, participant: L2 |
| FixedVarBirelationSet, VarVarBirelationSet | remove_participant_2 | position: ParticipantPosition |
| VarVarBirelationSet | insert_participant_1 | position: ParticipantPosition, participant: L1 |
| VarVarBirelationSet | remove_participant_1 | position: ParticipantPosition |

Positions are local to the selected factor. Replacement and removal address
an existing position. Insertion occurs before the selected position and
accepts the factor length to append. Insertion and removal preserve the
relative order of surviving participants and are available only for variable
factors. Single-participant replacement preserves factor length. These
conveniences share the whole-factor mutation and incidence-update machinery;
they do not introduce separate index-maintenance paths.

Graph-IR and editor methods that delegate to these primitives still need
their own surface settled. This list introduces no new traits, wrapper types,
or Python bindings.

### Approved naming — 2026-09-15

Collection operations act on the relation-set receiver. Operations on one
relation take RelationId first and name the affected component. Rename the
existing permutation methods to align with participant replacement:

| Current | Approved |
| --- | --- |
| permute_with | permute_participants |
| permute_1_with | permute_participants_1 |
| permute_2_with | permute_participants_2 |
| incident | incident_to_node |
| incident_edge | incident_to_edge |
| has_incident | has_incident_to_node |
| has_incident_edge | has_incident_to_edge |
| coincident | coincident_to_node |
| coincident_edge | coincident_to_edge |
| coincident_in (private) | coincident_among |

Apply the incidence names to private Incidence as well. is_coincident retains
its name and tests a supplied RelationId. Incidence and coincidence are
parallel queries with different matching criteria: reference membership and
complete per-factor participant-multiset equality, respectively. The supplied
node or edge restricts coincidence candidates. No find_ prefix is introduced.

Among the six overlay entity sets, only StereoBonds directly references both
atoms and bonds. Keep the other five entity sets' names unchanged. Rename
StereoBonds methods as follows:

| Current | Approved |
| --- | --- |
| incident_ids(atom) | incident_to_atom_ids(atom) |
| has_incident(atom) | has_incident_to_atom(atom) |
| incident_bond_ids(bond) | incident_to_bond_ids(bond) |
| has_incident_bond(bond) | has_incident_to_bond(bond) |

StereoBondViews uses incident_to_atom_ids, has_incident_to_atom, and
incident_to_atom for its corresponding queries. Preserve the existing scope
difference: StereoBonds atom incidence covers ligand anchors; the
molecule-aware views also include site-bond endpoints. The site-specific at,
at_id, and is_at methods retain their names. This decision adds no new view
methods and does not rename the role-specific filters or of / of_id methods.

### Remaining design

Doc 227 records a scratch feasibility experiment splitting relation storage and
unit tests by type with no new scoped visibility markers. Its ordering,
documentation, and property-reference findings inform this API review; the
experiment does not implement these renames or additions.

The accepted organization in doc 227 extracts public participant vocabulary
into participant.rs and permits an internal incidence.rs component using
pub(super) for its relation-subsystem interface. It retains explicit public
re-exports in relation.rs and small private shared functions where appropriate.
This supersedes treating zero additional visibility markers as a layout goal.

The graph-core signatures, failure contract, and initial method scope are
settled. The implementation plan below covers all five storage shapes and the
approved rename migrations. Graph-IR/editor delegation still needs its
own public surface settled before planning hydrogen integration. Doc 213's
generic editor-wrapper proposal is separate; the storage primitive must not
depend on introducing that proposed trait. Its editor design must accommodate
storage-owned participant mutation. Doc 211 remains the completed record of
the existing relation API; this document owns the replacement extension and
approved naming changes.

Hydrogen folding and unfolding are essentially designed above. Their
implementation is blocked on the mutation surface; the graph-core prerequisite
can proceed independently.

## Graph and relation restoration

Recorded 2026-09-18. Restoration extends the storage mutation surface described
above. The semantics, names, and interfaces below are settled. The 2026-09-18
contract correction treats restoration as undo: matching removal data restores
the original storage; manipulated inputs must not panic but have no specified
restoration result. S7a supplies Graph::restore, S7b supplies FixedRelationSet
restoration, S7c supplies VarRelationSet restoration, and S7d supplies
FixedFixedBirelationSet restoration. S7e supplies FixedVarBirelationSet restoration,
and S7f supplies VarVarBirelationSet restoration. The earlier S6a RestoreError has
been removed.
S6–S8 below cover the implementation.
The graph-IR/editor integration remains in
[213](213-editor-overlay-storage-2026-08-27.md#removal-rollback-current-implementation).

### Relation restoration

Relation storage owns two separate operation families across all five shapes:

| Name | Inputs and effect |
| --- | --- |
| restore | Relation compaction and saved removed rows. Recover original row positions, preserve surviving participants and attributes unchanged, and reinstate saved rows. No graph compaction is required. |
| restore_participants | GraphCompaction. Apply inverse reference translation to stored participants, preserving relation ids, participant positions, and attributes. |

Both operations maintain incidence indices. Compaction::uncompact and
RelationParticipant::uncompact already supply the inverse mappings; no new
participant trait operation is required. The existing editor restore_* methods
establish the restoration naming precedent. Its private restore_*_participants
helpers actually perform uncompaction, so their current names conflate the two
operations. The nomenclature guide distinguishes restoration of saved entities
from inverse mapping of survivors.

Compaction::uncompact and GraphCompaction::uncompact_node/uncompact_edge only
translate an id and return its original value; they do not mutate storage.
RelationParticipant::uncompact likewise returns a translated participant value.
Keep uncompaction non-mutating. Do not name a storage mutation
uncompact_participants. Applying translated references to stored participants and
updating incidence indices is the separate storage mutation restore_participants.

For topology rollback, first restore the surviving relations' participant references,
then restore removed relations. Saved removed rows already contain original
participant ids and must not be expanded again. Relation-only rollback needs
restore alone. Undoing participant replacement, insertion, or removal instead
uses the existing replace_participants methods with the saved factor contents;
any affected attributes are restored at the graph-IR level.

Exact rollback is bound to the removal history; restoration does not validate that
history again. A surviving payload changed since removal is preserved by restore;
recovering the original set requires original saved entries and unchanged survivors.

Existing removal APIs need not start returning journals: the transaction layer
can continue capturing rows before removal. Saved rows already use the set's
ordinary participant/payload shapes; a new storage journal type is not required
by this capability. Constraint restoration, session correspondence, cross-table
ordering, and transaction provenance remain graph-IR responsibilities.

### Graph restoration

Graph also owns restoration, through one bulk restore operation accepting a
GraphCompaction and saved removed edges with their original EdgeIds and endpoint
NodeIds. Nodes have no graph-owned payload, so the compaction supplies their
original positions and count. The operation restores the node id space, expands
surviving edge endpoints, restores original edge positions and saved edges, and
rebuilds adjacency storage. Graph continues to admit self-loops and parallel edges.

An identity compaction for the unaffected space permits node-only or edge-only
restoration. Separate restore_nodes and restore_edges conveniences are not required
for the initial capability. Graph owns both nodes and their internal edge references,
so restoring nodes includes translating surviving endpoints. Relation sets reference
an externally owned graph and keep participant-reference restoration separate
from row restoration.

Atom/bond attributes remain graph-IR-owned. The editor coordinates their restoration
with Graph::restore, relation participant-reference and row restoration, constraints,
and correspondence; it no longer reconstructs adjacency or relation storage itself.

### Approved interfaces and undo contract

All five relation-set types expose restore with &mut self, a borrowed
Compaction<RelationId>, and owned saved entries. For example, VarRelationSet:

```rust
pub fn restore(
    &mut self,
    compaction: &Compaction<RelationId>,
    removed: Vec<(RelationId, Vec<P>, D)>,
);
```

The original id precedes the existing constructor's entry fields:

| Storage shape | Saved entry |
| --- | --- |
| Fixed | `(RelationId, [P; N], D)` |
| Variable | `(RelationId, Vec<P>, D)` |
| Fixed/fixed | `(RelationId, [L1; N1], [L2; N2], D)` |
| Fixed/variable | `(RelationId, [L1; N1], Vec<L2>, D)` |
| Variable/variable | `(RelationId, Vec<L1>, Vec<L2>, D)` |

Entries may arrive in any order. Matching removal data covers compaction.removed()
exactly once and accompanies storage with compaction.result_count() surviving rows.
These relationships follow from removal and capture; restoration does not validate
them again. Explicit ids are retained rather than requiring entries to follow the
ordering of compaction.removed(). No new entry types are introduced solely for
these arguments.

Saved entries are consumed so payloads move into storage. Neither saved nor
surviving payloads require Clone. Participant order and payloads are preserved;
incidence is rebuilt for the restored relation ids. Relation restore retains the
ordinary storage admission contract for participant references: it does not
validate membership in an external graph.

All five relation-set shapes also expose:

```rust
pub fn restore_participants(
    &mut self,
    compaction: &GraphCompaction,
);
```

This applies to the whole set and both factors of birelations. Each referenced
node/edge from the matching removal belongs to the corresponding compaction result
domain. The method uses RelationParticipant::uncompact and preserves relation ids,
factor lengths, participant positions, and payloads while updating incidence. It
neither restores deleted participants nor reorders frames or transports payloads.

Graph exposes:

```rust
pub fn restore(
    &mut self,
    compaction: &GraphCompaction,
    removed: &[(EdgeId, [NodeId; 2])],
);
```

Matching removal data supplies the compaction and the original saved edges, in any
order. Saved endpoints use the original node id space. A borrowed slice suffices
because ids and endpoints are Copy; no node entries are needed.

All restoration methods return (). Their two requirements are:

1. Matching removal data reconstructs the original storage, including original ids.
2. Manipulated or mismatched inputs do not cause a panic. No correct restoration,
   diagnostic, or unchanged-receiver behavior is promised for those inputs.

Removal and capture establish the relationship between the receiver, compaction,
and saved entries. Separate arguments do not turn that undo operation into a
consumer of semantically independent inputs. This follows the operation-issued
provenance distinction in the [data-type guide](../docs/development/data-types.md#provenance-and-contextual-validity).
There is no RestoreError, checked counterpart, or tracked restoration variant.
The compaction already determines the survivor mapping.

### Guards against panics

Guard operations that could panic when undo data has been manipulated. Do not
re-establish the full removal history through a preliminary validation pass.

- Use checked inverse translation for graph survivors and protect saved-entry
  destination indexing. Check saved endpoint bounds before adjacency construction.
- Do not detect duplicates or require membership in the removed-id set. Exact
  current-count equality is not required merely to establish undo provenance.
- Handle missing rows without unwrapping absent entries or inventing generic
  payloads. No separate completeness scan or error-order contract is required.
- Before calling RelationParticipant::uncompact, inspect that participant's refs
  for coverage of the compaction result domain. Guard during traversal; do not
  add a separate whole-set validation pass or new participant trait method.
- Protect size arithmetic and allocation capacities from overflow. Keep internal
  storage coherent when stopping or skipping unusable input; the malformed-input
  contract does not specify which resulting storage value is chosen.

This replaces the earlier eleven-variant RestoreError and unchanged-on-error
proposal. Checks exist to prevent panics, not to diagnose compromised provenance.
The completed S6a work and initial S7a measurements are retained below as historical
records; the corrected implementation and measurements are recorded with S7a.

### Boundary cases and restoration laws

- Identity compaction with matching counts and empty saved entries makes restore
  a no-op. restore_participants with a matching identity also leaves storage unchanged.
- After complete removal, empty storage can restore the entire original graph or
  relation set. Graph restores isolated nodes from the compaction alone.
- Participant restoration on an empty relation set succeeds without changes;
  it does not recreate removed relations.

Matching removal and restoration must recover the original storage. Participant
compaction followed by restore_participants must recover surviving participant
values, with relation ids interpreted in the corresponding compacted row space.

The required graph-core additions are Graph::restore, restore and
restore_participants on all five relation-set types. Existing
uncompact interfaces and semantics remain unchanged. No new participant trait
methods, storage wrappers, or storage-owned transaction journals are needed. Edge-endpoint
replacement is a separate potential graph-core addition for bond rewiring,
not a restoration prerequisite.

## Entity-field invariant validation

Add one standalone, model-independent validator for the concrete domains of
inherent entity fields. The validator is additive in 0.7.1: it is not inserted
into the public `Validator` aggregate, whose public fields and contradiction
enum make that integration breaking.

The check covers only literal inherent values:

- atom charge fits `i8`; implicit-hydrogen and lone-pair counts fit `u8`;
- localized-bond order is one of 1, 2, 3, or 4, and its charge fits `i8`;
- dative-bond order fits `u8`;
- aromatic-system and multicenter-bond charges fit `i8`.

`SpinInvariantsValidator` already owns unpaired-electron count, multiplicity,
and their physical compatibility for atoms, localized bonds, aromatic systems,
and multicenter bonds. Element, isotope, noncovalent-kind, and stereo domains
are already represented by bounded types or enforced by `Molecule` integrity.
Electron-vector length is also an integrity property. The current specification
does not define an additional entity-local charge/electron conservation law for
aromatic or multicenter entities, so this work does not invent one.

A relevant non-literal field makes this focused validation underdetermined; an
out-of-domain literal is a typed contradiction naming the entity, field, value,
and required domain. The validator neither mutates nor resolves the molecule
and does not traverse inline, relational, or molecule-level constraints.

The narrow `EntityFieldInvariantsValidator` name avoids reviving the old
`EntityStructureValidator`: the latter mixed checks that now belong to
`Molecule` integrity with semantic validation.

## Stereo-ligand invariant validation

Add a standalone `StereoLigandInvariantsValidator` to check the
model-independent requirement that a stereo entity lists every ligand of its
site. For each stereo-atom frame:

- its actual-atom ligands are exactly the site's localized-bond neighbors;
- its number of virtual implicit-hydrogen ligands equals the site's literal
  `implicit_hydrogens` count;
- its number of virtual lone-pair ligands equals the site's literal
  `lone_pairs` count.

Apply the same rules independently to each endpoint block of a stereo bond,
excluding the opposite site endpoint from that block's actual-atom ligands.
An explicit hydrogen is an ordinary actual-atom ligand: it is present by atom
id in the frame, is not included in the implicit-hydrogen count, and is not a
virtual ligand.

A non-literal inherent count makes the corresponding equality
underdetermined. A literal mismatch or an incomplete or extraneous
actual-atom ligand set produces a typed
`StereoLigandInvariantsContradiction` naming the stereo entity, bearer atom,
and mismatch. Representation integrity already limits each virtual-ligand kind
to zero or one occurrence within a stereo-atom frame or stereo-bond endpoint.
Consequently, an inherent implicit-hydrogen or lone-pair count greater than one
contradicts an existing stereo entity because that entity cannot contain the
complete ligand census. This validator imposes no corresponding restriction on
an atom that is not the bearer of a stereo entity.

The check is tier 2, not `Molecule` integrity. An incomplete or inconsistent
frame remains structurally interpretable and does not make ordinary graph-IR
operations unsafe; it is a model-independent semantic contradiction.
The existing integrity rule allowing at most one equal virtual ligand within a
stereo-atom frame or stereo-bond endpoint remains unchanged. Producers of
stereo entities, including stereo perception, must construct the same complete
census. They collect every actual ligand and add a virtual ligand for each
implicit-hydrogen or lone-pair count of exactly one. A count greater than one
precludes producing the stereo entity; it is not truncated to one virtual
ligand.

`HydrogenUnfolder` enforces the implicit-hydrogen requirement directly as an
operation precondition and returns `UnfoldHydrogensError` on disagreement. It
does not require a prior validator call and does not inspect lone-pair support,
which its transformation does not change.

## Deferred counts-resolver shell-capacity correction

Recorded 2026-09-10 during the doc 226 stereo-input experiments. With
ValenceModel::smiles() (counts candidate generation and MostSaturated), both
`[C-](F)(Cl)(Br)(I)` and `[C@-](F)(Cl)(Br)(I)` currently resolve with zero
hydrogens, zero lone pairs, and one unpaired electron on carbon. Four bonds
plus that electron put nine electrons around carbon. Both inputs must be
rejected by valence resolution, independently of the stereo marker.

In `umol-graph/src/ops/valence/counts.rs`, literal hydrogen counts bypass the
saturation-target bonding budget; candidate generation balances the remaining
electron budget but admits this shell-capacity violation. Saturation targets
are hydrogen-completion targets, not an allowed-valence whitelist. The fix
must enforce the applicable shell capacity without conflating those contracts.
The design and implementation remain deferred; do not generalize carbon's
eight-electron bound to every element.

Future regression coverage must include both marked and unmarked reproducers,
valid neutral four-coordinate carbon, and three-coordinate carbanion controls. Differences
in the layer rejecting other stereo inputs do not require changes for this
work item. This correction is separate from, and does not block, the SMILES
roundtrip design.

## Implementation plan

This section collects the implementation stages for this document. The plan
began on 2026-09-15. S0–S5 cover the relation module split, storage mutation,
and graph-core/stereo-bond query renames; those stages are complete.

Restoration contracts are settled above; S6–S8 below implement them.
Hydrogen operations, validators, and the counts-resolver correction remain
unplanned; their implementation stages also belong here when ready. Graph-IR
participant mutation and editor delegation are designed and planned in
[213](213-editor-overlay-storage-2026-08-27.md). Doc 227's wider repository
restructuring is outside this plan.

For S0–S5, the public-symbol inventory is the approved additions and naming sections
above, including their listed variants across the five storage shapes, plus
the 15 methods specified in S5.
Existing public types retain their crate-root paths. There are no new public
types, traits, conversions, checked variants, payload bounds, or compatibility aliases.
Each subitem must reconcile its actual public diff with that inventory.

### Storage mutation implementation strategy

Initially, whole-factor replacement writes the selected row's participant
storage and rebuilds the derived Incidence once from the final stored rows.
Fixed factors replace their arrays; variable factors replace their contiguous
range and adjust subsequent offsets. Payload storage and relation numbering
remain untouched. Rebuilding incidence does not require reconstructing the
relation set through new, cloning payloads, or materializing editor entries.

For a birelation, the both-factor method is the mutation foundation. The
factor-specific methods preserve the other factor and delegate to it. Every
single-participant method constructs the changed factor and delegates to its
whole-factor method. Both-factor replacement rebuilds incidence only after
both final factors are installed. Keep the existing multiset-preserving
permutation implementation; this delegation requirement concerns the new
mutation family.

This deliberately starts with the existing incidence builder. Its cost depends
on the entire collection, and variable-factor resizing may move later
participants. Measure those costs explicitly; do not claim constant-time
replacement or representative molecular workloads from synthetic benchmarks.
Incremental incidence maintenance and specialized single-participant mutation
are follow-up optimizations, subject to evidence and a separate decision.

### S0 — Establish behavioral and performance baselines

- **S0a — Relation contract fixtures and properties** — completed 2026-09-15;
  additive (green).
  Modules: current relation unit tests and
  `umol-graph-core/tests/property/relation.rs`. [dep: none]
  Preserve the existing cases and transport laws. Establish independent
  reference expectations for stored order, duplicate participants, node/edge
  union incidence, and coincidence with repeated values. Include participants
  exposing both references or neither and references shared across factors.
  These tests exercise existing construction and queries, so they remain green
  before replacement exists. Use public APIs for external properties; expected
  incidence comes from a direct scan of fixture rows, not the storage index.
  Added 44 exact unit cases across the five shapes and five public-API properties.
  Exact cases cover preserved rows, duplicate participants and coincident rows,
  empty variable factors, multiplicity in either factor, and factor separation.
  Generated participants expose node references, edge references, both, or neither;
  independent labels distinguish complete participant equality from shared refs.
  Incidence expectations scan input rows, while coincidence expectations count
  occurrences without using the implementation's sorting/index machinery.
  Existing unit cases and the ten transport/iterator properties are retained;
  production code and public APIs are unchanged. No test modules import from
  one another, and no constructor shorthand or shared test module was added.

  Verification: 264 relation unit cases; the full graph-core suite (1,057 unit
  and 53 integration cases); all 15 relation properties with PROPTEST_CASES=256;
  strict all-target graph-core Clippy with proptest; and the locked Rust 1.87.0
  all-target graph-core check with proptest passed. Pinned nightly formatting
  and git diff --check passed. S0b was not part of that verification.
- **S0b — Relation benchmarks** — completed 2026-09-16; additive (green).
  Modules: new `umol-graph-core/benches/relation.rs` and its Cargo target.
  [dep: S0a] Use the existing Criterion dependency. Baseline construction,
  incidence queries, coincidence, and permutation for all five shapes. Include
  single-row and growing collections, short and longer variable factors, and
  sparse versus repeated references. Separate fixture setup from measurement.
  Record dimensions, compiler, commands, and baseline results in this document;
  use scratch only for disposable run artifacts. Retain benchmark sources in the
  crate. Add replacement measurements with each S2 subitem, using the same fixtures.

  Added the standalone Criterion relation target with 360 benchmark cases.
  All five shapes cover 1, 64, and 1,024 rows, with variable factors of length
  4 or 32. Sparse fixtures have distinct references; repeated fixtures share
  an anchor across rows and repeat references within factors. Coincidence
  measures last-row hits and absent queries through the same anchor. Birelations
  exercise node and edge indices and each factor's permutation separately.
  Fixture assertions check exact incidence and coincidence results before timing.
  Construction and permutation exclude setup cloning and final destruction;
  incidence measures returning the slice, not traversing it.

  The benchmark at commit 00ecf5bf2 defines all 360 baseline cases. This initial
  run used 20 samples, 100 ms warmup, and 200 ms requested measurement time per
  case; close comparisons need longer repeated runs.
  As an example of the measured candidate-list cost, fixed-relation coincidence
  hits at 1,024 rows averaged 53 ns with distinct anchors and 15.7 µs with the
  common anchor. These synthetic inputs do not establish molecular workload sizes
  or justify an incidence-update optimization before replacement is measured.

  Verification: full graph-core tests (1,057 unit and 53 integration cases),
  all 15 relation properties with PROPTEST_CASES=256, the complete benchmark run,
  strict Clippy for the relation benchmark, and its locked Rust 1.87.0 check
  passed. Pinned nightly formatting and git diff --check passed. Production APIs
  and existing tests are unchanged; S1a is next.

Gate: graph-core unit and integration tests, the explicit relation property
target, and a successful relation benchmark run. Record existing failures
separately; do not weaken assertions to establish a baseline.

### S1 — Split and document the relation family

- **S1a — Shared vocabulary and incidence** — completed 2026-09-16;
  structural (green).
  Modules: `relation.rs`, new `relation/participant.rs` and
  `relation/incidence.rs`, and crate-root declarations. [dep: S0a, S0b]
  Move RelationParticipant, ParticipantRefs, ParticipantPosition, and their
  implementations into participant.rs. Move Incidence into incidence.rs; use
  pub(super) only for its type and methods used by the relation subsystem,
  leaving fields and other details private. Keep RelationId, correspondence
  types, and small shared algebra helpers in the parent. Explicitly re-export
  intended public symbols; the relation module and its children remain private.
  Move participant unit tests into `relation/tests/participant.rs`, with their
  fixture defined locally. Preserve their assertions.

  Extracted participant.rs (104 lines), incidence.rs (77 lines), and the
  participant test file (106 lines). The relation parent is now 5,788 lines;
  storage implementations and their remaining tests await S1b. Incidence and
  its five methods use pub(super); its fields stay private. The relation module
  is private, with the previous crate-root public exports preserved.
  The participant tests import public production symbols and test tools
  explicitly and own a local copy of their mapping fixture. They import no
  other test module or parent test helpers.

  Preservation checks confirm unchanged participant/Incidence bodies apart
  from visibility and formatting, byte-for-byte unchanged storage implementations,
  and unchanged inputs and assertions in all eight moved test functions
  (17 expanded cases).
  Verification passed: 264 relation cases; the full graph-core suite
  (1,057 unit and 53 integration cases); all 15 relation properties with
  PROPTEST_CASES=256; all-target Clippy and Rust 1.87.0 checks with proptest;
  rustdoc with private items and warnings denied; pinned nightly formatting;
  and git diff --check. No mutation methods or renames were introduced. S1b
  is next; the S1 benchmark comparison remains part of the full stage gate.
- **S1b — Five storage implementations and their tests** — completed 2026-09-16;
  structural (green).
  Modules: `relation/{fixed,var,fixed_fixed,fixed_var,var_var}.rs` and matching
  `relation/tests/` files. [dep: S1a]
  Move each type with its inherent and trait implementations. Each test file
  owns its fixtures, test payload types, and assertion helpers, with explicit
  imports of production symbols and external test tools. Test modules must not
  import from one another. Accept small local duplication to keep them independent.
  Use constructors such as NodeId directly; remove shorthand helpers such as n
  rather than duplicating them. After relocation, `relation/tests.rs` contains only child-module
  declarations; it supplies no inherited imports or shared helpers. Do not add
  a common fixtures or utils module for this split. Keep properties outside src.
  Preserve behavior, trait coverage, and test cases; the observed Hash asymmetry
  does not authorize adding impls. Compare moved production bodies and expanded
  case identities against the baseline, and review test-support localization
  separately to ensure assertions and inputs retain their meaning. No sibling
  storage implementation or test module should depend on another sibling.

  Extracted all five storage implementations and their unit tests. The parent
  relation.rs is now 189 lines, and tests.rs contains only six module declarations.
  File sizes after formatting are:

  | Module | Implementation lines | Test lines |
  | --- | ---: | ---: |
  | fixed | 420 | 586 |
  | var | 449 | 714 |
  | fixed_fixed | 527 | 653 |
  | fixed_var | 566 | 790 |
  | var_var | 565 | 674 |

  Storage modules depend on shared participant vocabulary, Incidence, and parent
  algebra/matching helpers, with no dependencies on sibling storage implementations.
  Public storage types retain explicit parent and crate-root re-exports. No new
  visibility markers were needed; the parent helpers remain private. Cross-type
  algebra rustdoc links use crate-root paths. Each test module imports production
  symbols and test tools explicitly and owns its fixtures, payload types, and
  assertion helpers. There are no imports between test modules or shared test
  support modules; NodeId construction replaces the n shorthand.

  Preservation checks confirm unchanged production bodies and shared helpers,
  allowing formatting and qualified rustdoc links, and unchanged inputs and
  assertions in all 136 moved test functions after expanding NodeId construction.
  All 264 expanded relation case identities are preserved apart from module paths.
  Participant and incidence modules, participant tests, public root exports,
  external properties, and benchmarks are unchanged.

  Verification passed: 264 relation cases; the full graph-core suite (1,057 unit
  and 53 integration cases); all 15 relation properties with PROPTEST_CASES=256;
  all-target Clippy and Rust 1.87.0 checks with proptest; rustdoc with private items
  and warnings denied; pinned nightly formatting; and git diff --check. S1c is
  next; the benchmark comparison remains part of the full S1 gate.
- **S1c — Method order and current contracts** — completed 2026-09-16;
  documentation/structural (green). Modules: the relation family and its tests. [dep: S1b]
  Use the same order in all five implementations: construction and owned
  extraction; collection queries and iterators; per-relation readers;
  incidence and coincidence queries; mutations; graph-id transport and
  compaction; relation algebra. Keep mutable/read-only, checked/asserted, and
  plain/tracked peers adjacent within their groups. Keep trait implementations
  together in a consistent position and tests in source-definition order.
  Remove claims about obsolete ordering parameters, constructor sorting,
  canonical entries, incidence offset tables, or guaranteed unique coincidence.
  Document actual absence conditions, indexed-access and callback panic
  preconditions, and existing semantic laws with their preconditions. Repair
  intra-doc links after relocation. Identify the external property target from
  operation-family documentation and link property module comments back to
  public laws; do not create a second coverage inventory or cite this document
  from source. Any newly discovered semantic discrepancy requires a design
  decision, not a silent behavior change.

  Applied the agreed order to all five implementations and their unit tests.
  Correspondence carriers precede shared functions; trait implementations follow
  inherent methods. Reviewed module comments, replaced obsolete representation
  claims, and documented actual query absence, indexed-access panics, transport
  laws, and algebra callback requirements. Public property comments link to the
  corresponding public contracts. Public symbols, bounds, visibility, function
  bodies, test inputs, and assertions are unchanged; all 290 expanded relation
  case identities are preserved. S2a's replacement methods remain unchanged.

  File sizes after formatting are:

  | Module | Implementation lines | Test lines |
  | --- | ---: | ---: |
  | fixed | 584 | 836 |
  | var | 566 | 705 |
  | fixed_fixed | 669 | 653 |
  | fixed_var | 708 | 789 |
  | var_var | 707 | 674 |
  | participant | 143 | 106 |

  The parent is 200 lines, Incidence 91, and tests.rs six declarations. Dependencies
  remain as recorded in S1b: storage modules share vocabulary, Incidence, and private
  parent helpers; no storage implementation or test module imports a sibling.
  The only restricted visibility remains Incidence's six existing pub(super)
  declarations. No shared test-support module or new visibility was introduced.

  The full S1 gate passed: 1,083 unit and 53 integration cases; all 17 relation
  properties with PROPTEST_CASES=256; all-target Clippy and Rust 1.87.0 checks;
  warnings-denied rustdoc for the library, cfg(test) unit modules, and external
  property target, both with and without private items; pinned nightly formatting;
  and git diff --check.

  All 396 benchmark cases completed, and all 360 S0b cases were compared with
  the archived baseline. The median current/baseline mean ratio was 0.999;
  Criterion classified 49 cases as regressions and 30 as improvements. The
  largest slower cases were fixed_var construction at 1,024 rows and width 32:
  repeated references measured +54%, sparse references +40%. These short samples
  do not establish the cause of the differences or equivalent performance.

Gate: the S0 correctness gate, all-target compilation, rustdoc with warnings
denied and private items included, and benchmark comparison against S0. Record
actual file sizes and inter-module dependencies; the experiment's maximum of
595 lines is a baseline, not a mandated threshold for the expanded API.

### S2 — Implement the complete mutation family

Each subitem is additive (green), includes rustdoc, unit cases, public-API
properties, and benchmarks, and implements all approved methods for its type.
Factor helpers stay private. No method may expose an unindexed mutable
participant reference or require a new bound on D.

- **S2a — FixedRelationSet** — completed 2026-09-16; additive (green).
  Module: `relation/fixed.rs`. [dep: S1c]
  Add whole-factor and single-participant replacement. Include zero-arity
  construction and whole-factor replacement; every local position is invalid
  for zero arity.

  Added replace_participants and replace_participant with the approved signatures
  and panic contract. Whole-factor replacement writes the array and rebuilds
  Incidence once; single-participant replacement edits a copied array and delegates.
  Both preserve payloads without a Clone bound, row ids, and unrelated rows.
  References follow new's admission contract. No other public symbols, visibility
  changes, query renames, or editor wiring were introduced.

  Added 26 exact unit cases, including zero arity, node/edge references, repeated
  and shared references, sparse ids, unchanged and reordered frames, non-Clone
  positional payloads, and invalid ids/positions. Two external properties compare
  sequences of edits with independently edited rows and direct incidence scans;
  generated participants may expose a node, edge, both, or neither. Single-participant
  edits also agree with whole-factor replacement. Existing properties remain intact.

  Added 36 benchmark cases using the S0b fixed fixtures: both methods, 1/64/1,024
  rows, sparse/repeated references, and first/middle/last rows. Setup cloning and
  final destruction are excluded; the complete incidence rebuild is timed. A run
  after checks finished measured means of approximately 0.11 µs for one row and
  21–33 µs for 1,024 rows. These short synthetic measurements are not workload-size
  claims or optimization thresholds.

  Verification passed: 77 focused unit cases; all 17 relation properties with
  PROPTEST_CASES=256; full graph-core tests (1,083 unit and 53 integration cases);
  all-target Clippy and Rust 1.87.0 checks with proptest; strict private-item
  rustdoc; the 36 replacement benchmarks; pinned nightly formatting; and
  git diff --check. S2a was requested ahead of S1c; S1c and the full S1 gate are
  now complete.
- **S2b — VarRelationSet** — completed 2026-09-16; additive (green).
  Module: `relation/var.rs`. [dep: S1c]
  Add whole-factor replacement and single-participant replacement, insertion,
  and removal. Cover growth, shrinkage, empty factors, and later-row offset
  preservation.

  Added replace_participants, replace_participant, insert_participant, and
  remove_participant with the approved signatures and panic boundaries. Whole-factor
  replacement splices the selected flat range, adjusts subsequent offsets, and
  rebuilds Incidence once. All three local methods edit a copied factor and delegate.
  Payloads, relation ids, and unrelated rows are preserved. No new bound on D,
  visibility marker, constructor change, query rename, or editor wiring was added.

  Added 56 unit cases covering first/middle/last rows, growing and shrinking
  sequences, empty factors, duplicates, shared and sparse node/edge references,
  positional payloads, and invalid ids/positions. Default-empty cases compile all
  four methods for a non-Clone payload; new's existing Clone bound remains unchanged.
  Two external properties compare edit sequences against independent row models,
  checking every row, payload, incidence list, and coincidence result after each
  step. They also cover identity, local/whole equivalence, and insertion/removal
  roundtrips. Existing tests and properties are unchanged. The implementation is
  670 lines and its unit module 1,061; dependencies and visibility remain unchanged.

  Added and ran 252 benchmark cases: 1/64/1,024 rows, widths 4/32, sparse/repeated
  references, and first/middle/last rows. Whole-factor replacements have lengths
  zero, half, unchanged, and double; local methods operate on the first position.
  Each iteration starts with a fresh set, excluding setup cloning and final
  destruction. Measured means at 1,024 rows ranged from 22 to 255 µs across these
  fixtures. Timings include buffer movement, offset adjustment, and full incidence
  rebuilding; they do not isolate those costs or imply representative workloads.

  Verification passed: all 107 variable-relation unit cases; all 19 relation
  properties with PROPTEST_CASES=256; full graph-core tests (1,139 unit and 53
  integration cases); all-target Clippy and Rust 1.87.0 checks with proptest;
  strict rustdoc for library and test modules, with and without private items;
  the 252 mutation benchmarks; pinned nightly formatting; and git diff --check.
- **S2c — FixedFixedBirelationSet** — completed 2026-09-16; additive (green).
  Module: `relation/fixed_fixed.rs`. [dep: S1c]
  Add both-factor and factor-specific replacement, including the single-participant
  variants. Verify shared references survive replacement of just one factor.

  Added replace_participants, replace_participants_1, replace_participants_2,
  replace_participant_1, and replace_participant_2 with the approved signatures
  and panic boundaries. Both-factor replacement writes the two arrays and
  rebuilds Incidence once from their final union. Factor-specific methods retain
  the other array and delegate; local methods edit a copied array and delegate
  through the corresponding factor method. Payloads, row ids, count, and unrelated
  rows remain unchanged. No new payload bound, visibility, constructor, query name,
  or graph-IR/editor surface was introduced.

  Added 67 unit cases covering references retained across factors, fully removed
  references, unchanged/reordered/repeated arrays, sparse ids, distinct node/edge
  factor types, positional payloads, and invalid ids/positions. Whole-factor
  replacement permits either or both arities to be zero; local edits distinguish
  an empty selected factor from an empty other factor. Default-empty cases compile
  all five methods with a non-Clone payload; new's existing bound remains unchanged.
  Two external properties compare whole and mixed factor/local edit sequences
  with independent row models and direct union-incidence scans. Factor/local
  equivalence with both-factor replacement supplements those checks. Existing
  tests and properties are unchanged. The implementation is 776 lines and its
  unit module 1,283, with unchanged dependencies and visibility.

  Added and ran 90 benchmark cases using the S0b fixed/fixed fixtures: 1/64/1,024
  rows, sparse/repeated references, first/middle/last rows, and all five methods.
  Setup cloning and final destruction are excluded; the complete incidence
  rebuild is timed. Means ranged from 0.21–0.23 µs for one row and 32–54 µs for
  1,024 rows. These short synthetic measurements do not establish representative
  workloads or an optimization threshold.

  Verification passed: all 111 fixed/fixed-birelation unit cases; all 21 relation
  properties with PROPTEST_CASES=256; full graph-core tests (1,206 unit and 53
  integration cases); all-target Clippy and Rust 1.87.0 checks with proptest;
  strict rustdoc for library and test modules, with and without private items;
  the 90 mutation benchmarks; pinned nightly formatting; and git diff --check.
- **S2d — FixedVarBirelationSet** — completed 2026-09-16; additive (green).
  Module: `relation/fixed_var.rs`. [dep: S1c]
  Add all approved replacement methods and second-factor insertion and removal.
  Exercise node/edge mixtures and site/ligand-like shared anchors without
  introducing chemistry dependencies.

  Added replace_participants, replace_participants_1, replace_participants_2,
  replace_participant_1, replace_participant_2, insert_participant_2, and
  remove_participant_2 with the approved signatures and panic boundaries.
  Both-factor replacement writes the fixed array, splices the variable range,
  adjusts subsequent offsets, and rebuilds Incidence once from both final factors.
  Factor-specific methods preserve the other factor and delegate; local methods
  edit a copied array or vector and delegate through the corresponding factor
  method. Payloads, ids, count, and unrelated rows remain unchanged. No new
  payload bound, visibility, constructor, query name, or higher-layer surface
  was introduced.

  Added 94 unit cases covering growth, shrinkage, empty factors, first/middle/last
  rows, shared and fully removed references, sparse ids, repeated/reordered/unchanged
  sequences, mixed node/edge factors, positional payloads, and invalid ids/positions.
  Zero fixed arity permits variable-factor mutation, including insertion into an
  empty factor. Default-empty cases compile all seven methods with a non-Clone
  payload; new's existing bound remains unchanged. Two external properties compare
  whole and mixed factor/local edit sequences with independent row models, direct
  node/edge union scans, and occurrence-count coincidence checks. Generated
  participants can have distinct values sharing anchors across factors. Identity,
  factor/local equivalence, and insertion/removal roundtrips supplement those
  checks. Existing cases and properties are unchanged. The implementation is 874
  lines and its unit module 1,638; dependencies and visibility are unchanged.

  Added and ran 468 benchmark cases using the S0b fixed/variable fixtures:
  1/64/1,024 rows, variable widths 4/32, sparse/repeated references, and
  first/middle/last rows. Both-factor and second-factor replacement use lengths
  zero, half, unchanged, and double; local operations select position zero.
  Setup cloning and final destruction are excluded. Timings include the full
  incidence rebuild, variable-buffer movement, offset adjustment, and copies
  made by convenience methods. Means at 1,024 rows ranged from 25–263 µs.
  These short synthetic measurements do not isolate buffer/index costs or imply
  representative workloads.

  Verification passed: all 150 fixed/variable-birelation unit cases; all 23 relation
  properties with PROPTEST_CASES=256; full graph-core tests (1,300 unit and 53
  integration cases); all-target Clippy and Rust 1.87.0 checks with proptest;
  strict rustdoc for library and test modules, with and without private items;
  the 468 mutation benchmarks; pinned nightly formatting; and git diff --check.
- **S2e — VarVarBirelationSet** — completed 2026-09-16; additive (green).
  Module: `relation/var_var.rs`. [dep: S1c]
  Add all approved replacement, insertion, and removal methods. Cover either
  or both factors changing length, including both becoming empty.

  Added replace_participants, replace_participants_1, replace_participants_2,
  replace_participant_1, replace_participant_2, insert_participant_1,
  insert_participant_2, remove_participant_1, and remove_participant_2 with the
  approved signatures and panic boundaries. Both-factor replacement splices both
  buffers, adjusts their later offsets independently, and rebuilds Incidence once
  from the final union. Factor-specific methods copy the other factor and delegate;
  local methods edit a copied vector and delegate through the corresponding factor
  method. Payloads, ids, count, and unrelated rows are preserved. No new payload
  bound, visibility, constructor, query name, or higher-layer surface was introduced.

  Added 118 unit cases covering either or both factors growing, shrinking, or
  emptying; one growing while the other shrinks; first/middle/last rows; shared and
  fully removed references; sparse ids; repeated/reordered/unchanged sequences;
  mixed node/edge factors; positional payloads; and invalid ids/positions. Local
  operations work with the other factor empty, and removing a sole participant can
  leave both factors empty. Default-empty cases compile all nine methods with a
  non-Clone payload; new's existing bound remains unchanged. Two external properties
  compare whole and mixed factor/local edit sequences with independent row models,
  direct union scans, and occurrence-count coincidence checks. Both variable lengths
  vary independently. Identity, factor/local equivalence, and insertion/removal
  roundtrips supplement the model; roundtrips also check restored incidence.
  Existing cases and properties are unchanged. The implementation is 930 lines
  and its unit module 1,644; dependencies and visibility are unchanged.

  Added 720 benchmark cases using the S0b variable/variable fixtures: 1/64/1,024
  rows, initial widths 4/32, sparse/repeated references, and first/middle/last rows.
  Each factor-specific replacement uses lengths zero, half, unchanged, and double.
  Both-factor replacement covers both empty, both half, both unchanged, both double,
  and mixed half/double pairs. All six local operations select position zero.
  Setup cloning and final destruction are excluded; full incidence rebuilding,
  buffer movement, offset adjustment, and convenience copies are timed. Means at
  1,024 rows ranged from 33–457 µs. These short synthetic measurements do not
  isolate buffer/index costs or establish representative workloads. All 720 cases
  ran as part of the full S2 benchmark gate recorded below.

  All 163 focused unit cases and all 25 relation properties passed, with the
  properties included in the full graph-core property run. The complete S2 gate
  is recorded below.

For every shape, exact cases cover replacement with the same sequence,
reordering, repeated participants, newly added and fully removed references,
and references retained elsewhere in the relation. Check the complete stored
rows, payloads, relation ids, affected and unaffected incidence, and coincidence
results. Test invalid relation ids and local-position boundaries, including
insertion at length and removal from an empty factor. Fixed arity remains a
type constraint, not a runtime error case. Include payloads without Clone to
ensure mutation itself imposes no such bound; existing constructor bounds are
outside this change.

Properties compare the public result against an independently edited row
model: exact sequences and payloads agree, incidence equals a reference scan,
and unrelated rows remain unchanged. Factor-specific edits agree with the
corresponding both-factor edit; local edits agree with constructing and
replacing the whole factor. The row model supplies independent evidence in
addition to these delegation-equivalence checks. Include sequences of edits
that grow and shrink buffers, and preserve the existing transport properties.

Benchmarks measure whole-factor and local operations across collection sizes,
replacement lengths, and first/middle/last rows. Keep setup and cloning outside
the timed operation and prevent repeated insertion from growing inputs without
bound. Report full-index rebuild costs and variable-buffer movement separately
where the fixtures permit; do not introduce an optimization acceptance number
without measured evidence.

Gate after each subitem: focused cases and relation properties. Stage gate:
full graph-core tests, all feature-gated graph-core properties, strict Clippy,
rustdoc, and all relation benchmarks.

The full S2 gate passed on 2026-09-16: 1,418 graph-core unit cases and 53 integration
cases; all 152 graph-core property cases with PROPTEST_CASES=256; all-target Clippy
and Rust 1.87.0 checks with proptest; strict rustdoc for the library, unit modules,
and external property target, each with and without private items; all 1,926
relation benchmarks; pinned nightly formatting; and git diff --check. The benchmark
run includes all 1,206 prior cases and the 720 S2e additions. All five relation-set
mutation families are implemented. Editor delegation and hydrogen
operations remain separate follow-ups.

### S3 — Apply approved names and migrate callers

- **S3a — Graph-core method names** — completed 2026-09-17; breaking (red→green).
  Modules: the five relation implementations, Incidence, and every workspace
  caller. [dep: S2a, S2b, S2c, S2d, S2e]
  Apply the complete approved graph-core rename table, including private
  coincident_among. Migrate source, tests, benchmarks, examples, and living
  documentation within this subitem. Preserve historical discussion records.
  Do not rename unrelated graph or entity methods with similar spellings.
  Test assertions and exercised behavior remain unchanged.

  Applied all 38 public method renames across the five relation sets, the four
  private Incidence renames, and the five private coincident_among renames.
  Migrated graph-IR storage calls, unit cases, external properties, benchmark
  calls and labels, rustdoc links, and the living data-types guide. StereoBonds
  and StereoBondViews public renames are covered by S3b. No compatibility aliases,
  visibility changes, new bounds, or failure-contract changes were introduced.
  A full-diff audit confirms only approved renames and formatting in Rust;
  test inputs/assertions, property laws, and benchmark fixtures are preserved.

  Verification passed: all-feature workspace tests (33,098 passed, 6 ignored),
  doctests, all-target Clippy with warnings denied, strict workspace rustdoc,
  and the pinned Rust 1.87.0 all-target check; all 152 graph-core properties with
  PROPTEST_CASES=256; strict graph-core library, unit-module, and external-property
  rustdoc, each with and without private items; pinned nightly formatting; and
  git diff --check. Receiver-aware searches found no stale storage names.
  Benchmark targets compile; S2's timings remain the performance baseline.
- **S3b — Stereo-bond query names** — completed 2026-09-17; breaking (red→green).
  Modules: graph-IR StereoBonds, StereoBondViews, and their callers.
  [dep: S3a] Apply only the approved atom/bond names. Migrate callers in the
  same subitem. Preserve the distinction between storage ligand incidence and
  view incidence including site-bond endpoints; retain exact cases that expose
  this distinction. Leave the other five entity sets' public names intact.

  Applied the four StereoBonds and three StereoBondViews renames. Migrated
  internal view queries, reaction dangling-reference checks, the reaction
  property generator, and the existing exact view incidence cases. Storage
  rustdoc now states that atom incidence covers ligand anchors, including
  virtual ligands; views still include site-bond endpoints and deduplicate ids.
  Role-specific filters, site lookups, and other entity/view names are unchanged.
  Python stereo views do not expose these incidence methods. The full-diff audit
  confirms seven public renames with unchanged query bodies, signatures apart
  from names, visibility, test inputs/assertions, and property laws.

  Verification passed: all 71 stereo-view unit cases; all 152 graph-core
  properties with PROPTEST_CASES=256; all-feature workspace tests (33,098 passed,
  6 ignored), doctests, all-target Clippy with warnings denied, strict workspace
  rustdoc, and the pinned Rust 1.87.0 all-target check; pinned nightly formatting;
  and git diff --check. Receiver-aware searches found no stale stereo-bond
  callers. S3 is complete.

Gate: both subitems finish green with their migrated callers. Run the workspace
test and lint gate, explicit graph-core properties, and rustdoc. Search for old
names with receiver context to distinguish missed migrations from unrelated
APIs; do not use a workspace-wide textual replacement.

### S4 — Verify and record the delivered prerequisite

- **S4a — API and structural closeout** — completed 2026-09-17;
  verification/documentation (green).
  Modules: relation family, changed graph-IR surfaces, docs 166/227 and status
  index. [dep: S3b]
  Review the full diff against the symbol inventory, delegation requirement,
  visibility boundary, and preserved test laws. Record final production/test
  file sizes, shared interfaces, code versus rustdoc dependencies, benchmark
  evidence, and verification commands. Mark these subitems complete only on
  that evidence. Completing this prerequisite does not complete doc 166's
  hydrogen/validator work or doc 227's wider program. Update their remaining
  actions accordingly.

  Reviewed c76f79e5f (before S0a) through 6fc37b0d9 (S3b). All 11 existing
  crate-root relation exports retain their paths. The additions are exactly
  the approved 27 mutation methods (2/4/5/7/9 across the five shapes), with no
  new type, constructor, conversion, payload bound, or error surface. Five
  whole-factor methods rebuild incidence once; factor/local conveniences
  delegate through those methods. Invalid ids/positions panic, while reference
  admission remains the same as new. The original permutation path is preserved.

  Preservation checks reconcile all 201 original production function signatures
  and bodies after the approved names and visibility normalization, all 134
  original unit-test bodies and case-attribute groups, and all original external
  property functions. S3 delivers 38 public graph-core and seven public graph-IR
  renames. Query bodies, incidence scopes, trait coverage, test inputs/assertions,
  and existing property laws are preserved. No implementation correction was
  required by this closeout.

  Final line counts include comments and blank lines:

  | Component | Production lines | Unit-test lines |
  | --- | ---: | ---: |
  | relation.rs | 200 | — |
  | participant | 143 | 106 |
  | incidence | 91 | — |
  | fixed | 584 | 858 |
  | var | 670 | 1,080 |
  | fixed_fixed | 781 | 1,294 |
  | fixed_var | 879 | 1,653 |
  | var_var | 935 | 1,655 |
  | Total | 4,283 | 6,646 |

  tests.rs adds six module declarations. The external relation property module
  is 2,190 lines and the benchmark 1,685. These measurements include the complete
  mutation family and its contract/test coverage; they are not file-size limits.

  Storage implementations depend on participant vocabulary, Incidence, and the
  parent's ids, correspondence carriers, and five private functions. None depends
  on a sibling storage implementation. Algebra contracts are now self-contained;
  the experiment's cross-type rustdoc links are gone. Parent and external-property
  documentation links to public laws are documentation relationships only.
  Incidence's type, build, and four queries are the only six pub(super) declarations;
  there is no pub(crate) or pub(in ...) in the relation subtree. Fields stay private.
  Unit files own their fixtures/support and import public production symbols and
  test tools explicitly, with no test-module cross-imports or shared support module.
  The agreed method/trait/test order and documented failure/semantic contracts remain.

  The S2 benchmark run completed all 1,926 cases; source hashes checked during
  closeout matched commit 55549e0a7. The current benchmark differs only by S3a
  names and formatting. It measures complete mutation costs, including full-index rebuilding
  and variable-buffer movement, without isolating those costs. S1's median ratio
  across the original 360 cases was 0.999, but its recorded regressions (up to about
  54%) remain evidence requiring longer controlled runs before attributing a cause.
  No performance-equivalence claim or optimization threshold follows from these
  short synthetic measurements.

  The final source is the source checked by S3b: 33,098 workspace test cases passed
  (6 ignored), all 152 explicit graph-core properties passed with PROPTEST_CASES=256,
  and doctests, all-target/all-feature Clippy, strict workspace rustdoc, and the
  pinned Rust 1.87.0 check passed. S3a's six strict graph-core rustdoc configurations
  also remain applicable. S4a changes only these discussion records, so those broad
  gates were reused. Fresh preservation, delegation, visibility, benchmark-source,
  formatting, and diff checks passed. The durable measurements and reproduction
  instructions follow below; no scratch artifact is required by this record.

  S0–S4 are complete. Doc 166 remains In Progress for the follow-ups below and its
  remaining validator/counts work; doc 227 remains Proposed for its wider program.

#### Retained measurements and reproduction

This document retains the conclusions, source identities, measurement summaries,
and verification procedure. Scratch logs, raw timing samples, and one-off audit
scripts are disposable and are not part of the delivered prerequisite. Deleting
them removes the original samples; a later statistical comparison requires new
runs rather than assuming a saved Criterion baseline still exists.

The benchmark sources are retained in git:

| Measurement | Revision containing the benchmark | Cases |
| --- | --- | ---: |
| S0b baseline | 00ecf5bf2 | 360 |
| S1 comparison, including S2a | ae12c59f2 | 396 |
| Complete S2 mutation family | 55549e0a7 | 1,926 |
| Final approved names and callers | 6fc37b0d9 | 1,926 |

Measurements used rustc 1.96.0 (ac68faa20), aarch64-apple-darwin, with 20 samples,
100 ms warmup, 200 ms requested measurement time, and 10,000 bootstrap resamples.
No affinity or thermal controls were applied. Fixtures use 1/64/1,024 rows,
variable widths 4/32, sparse/repeated references, and first/middle/last edited
rows. Input preparation/cloning and final set destruction are outside timing;
index rebuilding/destruction, buffer movement, offset adjustment, and copies
made by convenience methods are included. Incidence queries measure returning
the slice, not traversing it. The maintained benchmark source defines every case.

The complete S2 run measured these ranges across mutation methods, edited rows,
reference patterns, and replacement sizes at 1,024 rows:

| Shape | Total benchmark cases | Range of measured means (µs) |
| --- | ---: | ---: |
| FixedRelationSet | 66 | 21.0–32.4 |
| VarRelationSet | 312 | 22.4–253.7 |
| FixedFixedBirelationSet | 144 | 31.8–54.1 |
| FixedVarBirelationSet | 576 | 23.0–213.0 |
| VarVarBirelationSet | 828 | 33.4–457.5 |

These ranges are descriptive summaries, not confidence intervals or application
workload claims. For the 360 original cases, the S1 comparison classified 204 as
no detected change, 77 within Criterion's noise threshold, 49 as regressions,
and 30 as improvements. The largest fixed/variable construction changes at
1,024 rows and width 32 were 105.9→162.8 µs for repeated references and
131.3→184.3 µs for sparse references. The overall median mean ratio was 0.999;
this does not cancel the regressions or establish their cause.

To obtain fresh measurements, run the following command at the chosen revision.
Use a different baseline name for each run; generate both runs afresh before
requesting a Criterion comparison. Renamed operation labels in S3a require
matching cases using the approved rename table above.

```sh
cargo bench --locked -p umol-graph-core --bench relation -- \
  --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
  --nresamples 10000 --noplot --save-baseline relation-current
```

The verification can be repeated with the commands below, with the repository
Python 3.13 environment activated. These commands regenerate the evidence from
the maintained source; they do not need saved logs or binaries.

```sh
source umol-py/.venv/bin/activate
python --version
cargo test --locked -p umol-graph-core
cargo test --locked -p umol-graph-ir --lib ir::view::stereo::tests
PROPTEST_CASES=256 cargo test --locked -p umol-graph-core --features proptest --test property
cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
cargo test --locked --workspace --all-features --tests --no-fail-fast
cargo test --locked --workspace --all-features --doc
RUSTDOCFLAGS='-D warnings' cargo doc --locked --workspace --all-features --no-deps
cargo +1.87.0 check --locked --workspace --all-targets --all-features
cargo +nightly-2026-06-04 fmt --all -- --check
git diff --check
```

For the six graph-core rustdoc checks, first generate fresh dependency artifacts
with cargo test --locked -p umol-graph-core --features proptest --no-run
--message-format=json. Read the .rlib filenames from its compiler-artifact records.
Use cargo rustdoc --locked -p umol-graph-core --features proptest with each target
below, passing the rustdoc arguments after -- and each required dependency as
--extern name=path using those fresh filenames. Set RUSTDOCFLAGS to -D warnings.
Run each configuration once normally and once with --document-private-items.

| Target arguments | Additional rustdoc arguments | Required --extern names |
| --- | --- | --- |
| --lib | — | — |
| --lib | --cfg test | rstest, pretty_assertions, proptest, serde, toml, xxhash_rust |
| --test property | — | umol_graph_core |

The preservation audit compared c76f79e5f with 6fc37b0d9: function signatures and
bodies after removing comments/formatting and applying the approved rename table;
original rstest attributes and case inputs after replacing n(...) with NodeId(...)
and qualifying imports through public re-exports; and original external property
functions. Function order was allowed to change. Added methods were checked
against the 27-method inventory and their delegation targets; scoped visibility
was counted separately. These are the audit criteria, not a dependency on the
temporary script used to perform the comparison.

At S3/S4/S5, use the repository workspace gates and the pinned CI MSRV check;
follow the python-build skill and confirm the repository Python 3.13 environment
before any workspace command including umol-py. Run feature-gated properties
explicitly. Format, run git diff --check, and inspect the full diff before
declaring a stage complete. Repeat broad checks only for subsequent changes
that invalidate their evidence.

### S5 — Add and remove whole relations — approved 2026-09-17

S5a–S5e are complete as of 2026-09-18. Each of the five relation sets now provides
add, remove, and tracked_remove. This stage covers storage only; editor wiring
and its public mutation surface remain separate follow-up work.

Every add takes &mut self, followed by the arguments below, and returns RelationId:

| Set | Arguments after &mut self |
| --- | --- |
| FixedRelationSet | participants: [P; N], data: D |
| VarRelationSet | participants: &[P], data: D |
| FixedFixedBirelationSet | participants_1: [L1; N1], participants_2: [L2; N2], data: D |
| FixedVarBirelationSet | participants_1: [L1; N1], participants_2: &[L2], data: D |
| VarVarBirelationSet | participants_1: &[L1], participants_2: &[L2], data: D |

Addition appends one relation and returns the previous count as its id. Existing
ids, participants, and payloads remain unchanged. The supplied participant order
and multiplicity are preserved; coinciding relations are permitted. Fixed arity
is enforced by the argument type, and variable factors may be empty. References
are indexed through RelationParticipant::refs without checking membership in an
external graph, matching new and participant replacement. Incidence is correct
before return and spans both factors of a birelation. The payload is moved into
storage; no D: Clone bound is introduced. There is no tracked addition variant:
the append layout and returned id fully describe the id change.

All five sets have the same removal signatures:

```rust
pub fn remove(&mut self, ids: &[RelationId]);

pub fn tracked_remove(&mut self, ids: &[RelationId]) -> Compaction<RelationId>;
```

Removal interprets every supplied id in the pre-removal set. Input order and
repetitions do not matter. Survivors retain their relative order, participant
sequences, and payloads; their relation ids become dense, and incidence uses
those new ids. Participant node/edge ids do not change. Empty input is a no-op,
with an identity compaction over the original count. Any out-of-range relation
id panics; validate all supplied ids before mutation. Plain remove delegates to
tracked_remove and discards its compaction. Single-relation removal uses &[id].
No payload Clone bound is needed. The returned compaction records the source
count and survivor mapping, not removed payloads or an undo journal.

These methods remove whole relations. Existing remove_participant methods edit
a factor within one relation. Removing a relation does not remove its referenced
nodes or edges. Existing compact/tracked_compact handle graph compaction and the
resulting removal of dependent relations.

Initially, storage updates its columns and variable-factor offsets directly,
then rebuilds Incidence once per addition or nonempty removal batch. It must not
reconstruct the set through new or clone payloads. Measure the complete mutation,
including the rebuild; repeated additions must not be described as constant-time.
Keep the approved module organization and visibility unchanged.

Each subitem below is additive (green), supplies rustdoc with semantic and panic
contracts, and includes its own unit cases, external public-API properties, and
benchmarks from the start. Test modules remain independent, with explicit imports
and no shared test-support construction. Shared coverage requirements are:

- Addition to empty/nonempty sets; zero fixed arity and empty variable factors;
  repeated participants, coinciding rows, and node/edge references shared across
  participants and factors; preservation of existing entries and non-Clone payloads.
- Empty, first, middle, last, multiple, and all-row removals; unordered/repeated
  input ids; exact survivor entries, incidence, and count-bearing compactions;
  invalid-id panic before mutation, including mixed valid/invalid requests.
- Independent row-model properties for mixed addition, participant replacement,
  and removal sequences, with incidence checked by direct reference scans;
  tracked/plain agreement and removal of an appended row restoring the prior set.
- Benchmarks across the existing row-count, variable-width, and reference-pattern
  dimensions, including an empty initial set and single/bulk removal. Keep input
  preparation outside timing and retain measurement summaries and reproduction
  instructions here; scratch outputs remain disposable.

Subitems:

- **S5a — FixedRelationSet** — completed 2026-09-18; additive (green).
  Module: `relation/fixed.rs`. [dep: S4a]
  Implement add, remove, and tracked_remove for the fixed participant column.
  Include zero-arity rows, which have payloads and ids despite empty incidence.

  Added the three approved methods without new types, visibility changes, or
  payload bounds. Addition appends to the participant and payload columns;
  removal validates the complete request through Compaction before moving
  survivors in order and truncating both columns. Each nonempty mutation
  rebuilds Incidence once. Empty removal returns identity without rebuilding.
  Plain remove delegates to tracked_remove. Neither operation reconstructs
  through new or clones payloads.

  Added 21 exact unit cases and two external properties. Cases cover empty and
  zero-arity storage, coinciding rows, repeated/shared/sparse references,
  non-Clone payloads, ordered compaction, repeated removal ids, and both removal
  methods rejecting invalid ids before changing rows or incidence. Properties
  compare mixed add/replacement/removal sequences with independent rows and
  direct incidence scans, and check add/remove restoration separately.

  All graph-core checks passed: 1,439 unit cases, 54 integration cases, all
  154 properties with PROPTEST_CASES=256, all-target Clippy with proptest and
  warnings denied, Rust 1.87.0 all-target checking with proptest, and the six
  strict rustdoc configurations described above. Pinned nightly formatting and
  git diff --check passed. The full workspace gate remains at the S5 boundary.

  Added and ran 84 benchmarks in relation/fixed_rows, with 0/1/64/1,024 initial
  rows, arity four, usize payloads, sparse/repeated references, and empty,
  first/middle/last, alternating, and all-row removal requests. Input cloning
  and final set destruction are outside timing; storage changes, removed-payload
  destruction, index rebuilding, and compaction construction are included.
  Tracked-result destruction is outside timing; plain removal discards its
  compaction during the operation. On rustc 1.96.0 (ac68faa20), aarch64-apple-darwin,
  the ranges of measured means at 1,024 rows were:

  | Operation | Range of means |
  | --- | ---: |
  | add | 23.3–27.5 µs |
  | Single-row remove/tracked_remove | 18.5–25.8 µs |
  | Alternating-row remove/tracked_remove | 14.7–17.4 µs |
  | All-row remove/tracked_remove | 6.98–7.09 µs |
  | Empty remove/tracked_remove | 5.07–5.70 ns |

  These short synthetic measurements are descriptive, not workload claims or
  optimization thresholds. Reproduce them with the maintained benchmark source
  and the command below; use a fresh baseline name for a later comparison.
  No scratch output is required by this record.

  ```sh
  cargo bench --locked -p umol-graph-core --bench relation -- \
    'relation/fixed_rows/' --warm-up-time 0.1 --measurement-time 0.2 \
    --sample-size 20 --nresamples 10000 --noplot --save-baseline s5a-2026-09-18
  ```

- **S5b — VarRelationSet** — completed 2026-09-18; additive (green).
  Module: `relation/var.rs`. [dep: S4a]
  Implement the three methods for the flat participant buffer and row offsets.
  Cover adjacent empty/nonempty rows and offset changes after bulk removal.

  Added add, remove, and tracked_remove with the approved signatures, without
  new types, visibility changes, or payload bounds. Addition extends the flat
  buffer and appends its end offset and payload. Removal validates every id
  through Compaction before copying surviving ranges forward, updating offsets,
  moving payloads, and truncating the columns. Empty rows remain distinct
  survivors with their payloads unless selected for removal. Both mutations
  rebuild Incidence once; empty removal returns identity without rebuilding. Plain remove delegates
  to tracked_remove. No reconstruction through new or payload cloning occurs.

  Added 24 exact unit cases and two external properties. Cases include adjacent
  empty/nonempty rows, removing either group or every row, repeated/out-of-order
  removal ids, node/edge incidence, non-Clone payloads, and rejection of invalid
  ids before mutation. Properties compare mixed addition/replacement/removal
  sequences against independent rows, with independently varying participant
  and payload lengths, and check add/remove restoration. Public readers and
  direct incidence scans check row boundaries and both reference spaces.

  Verification passed: all 1,463 graph-core unit cases, 54 integration cases,
  all 156 properties with PROPTEST_CASES=256, all-target Clippy with proptest and
  warnings denied, Rust 1.87.0 all-target checking with proptest, and all six
  strict rustdoc configurations. Pinned nightly formatting and git diff --check
  passed. The full workspace gate remains at the S5 boundary.

  Added and ran 252 benchmarks in relation/var_rows, using 0/1/64/1,024 initial
  rows, widths 0/4/32, usize payloads, sparse/repeated references, and the same
  removal selections as S5a. Width zero measures rows with payloads but no
  participants. Timing boundaries match S5a and include participant movement
  and offset updates. On rustc 1.96.0 (ac68faa20), aarch64-apple-darwin, ranges
  of measured means at 1,024 rows were as follows (all values in µs; removal
  ranges include both plain and tracked forms):

  | Width | add | Single-row removal | Alternating-row removal | All-row removal |
  | ---: | ---: | ---: | ---: | ---: |
  | 0 | 2.11–2.12 | 4.68–4.88 | 7.53–7.78 | 6.92–7.21 |
  | 4 | 25.0–28.4 | 25.2–30.7 | 18.6–20.3 | 7.03–7.21 |
  | 32 | 117–157 | 115–141 | 66.0–82.1 | 7.17–7.37 |

  Empty-removal means spanned 5.07–23.0 ns across these fixtures. These short
  synthetic runs are descriptive; they do not establish workload sizes or
  optimization thresholds. Reproduce with the maintained benchmark source and
  the command below, choosing a fresh baseline name for later comparisons.
  No scratch artifact is required by this record.

  ```sh
  cargo bench --locked -p umol-graph-core --bench relation -- \
    'relation/var_rows/' --warm-up-time 0.1 --measurement-time 0.2 \
    --sample-size 20 --nresamples 10000 --noplot --save-baseline s5b-2026-09-18
  ```

- **S5c — FixedFixedBirelationSet** — completed 2026-09-18; additive (green).
  Module: `relation/fixed_fixed.rs`. [dep: S4a]
  Implement the three methods across both fixed columns. Cover either or both
  factors empty and shared references across factors.

  Added add, remove, and tracked_remove with the approved signatures and no new
  types, visibility changes, or payload bounds. Addition appends both participant
  arrays and the payload. Removal validates the entire request through Compaction
  before moving both participant columns and payloads together in survivor order
  and truncating all three. Incidence is rebuilt once over both final factors;
  empty removal returns identity without rebuilding. Plain remove delegates to
  tracked_remove. No reconstruction through new or payload cloning occurs.

  Added 25 exact unit cases and two external properties. Cases cover either or
  both factors empty, coinciding rows, references shared within/across factors,
  mixed node/edge factors, sparse ids, non-Clone payloads, stable row compaction,
  repeated/out-of-order removal ids, and invalid-id rejection before mutation.
  Properties compare mixed addition/replacement/removal sequences with independent
  rows and direct union-incidence scans, and separately check add/remove restoration.
  Generated participants may reference nodes, edges, both, or neither.

  Verification passed: all 1,488 graph-core unit cases, 54 integration cases,
  all 158 properties with PROPTEST_CASES=256, all-target Clippy with proptest and
  warnings denied, Rust 1.87.0 all-target checking with proptest, and all six
  strict rustdoc configurations. Pinned nightly formatting and git diff --check
  passed. The full workspace gate remains at the S5 boundary.

  Added and ran 84 benchmarks in relation/fixed_fixed_rows, using 0/1/64/1,024
  initial rows, two arity-four factors (NodeId and EdgeId), usize payloads,
  sparse/repeated references, and the same removal selections and timing boundaries
  as S5a. On rustc 1.96.0 (ac68faa20), aarch64-apple-darwin, ranges of measured
  means at 1,024 rows were:

  | Operation | Range of means |
  | --- | ---: |
  | add | 38.4–46.6 µs |
  | Single-row remove/tracked_remove | 33.1–44.5 µs |
  | Alternating-row remove/tracked_remove | 22.1–26.8 µs |
  | All-row remove/tracked_remove | 6.92–7.25 µs |
  | Empty remove/tracked_remove | 7.83–8.67 ns |

  These short synthetic measurements are descriptive, not workload claims or
  optimization thresholds. Reproduce with the maintained benchmark source and
  the command below, choosing a fresh baseline name for later comparisons.
  No scratch artifact is required by this record.

  ```sh
  cargo bench --locked -p umol-graph-core --bench relation -- \
    'relation/fixed_fixed_rows/' --warm-up-time 0.1 --measurement-time 0.2 \
    --sample-size 20 --nresamples 10000 --noplot --save-baseline s5c-2026-09-18
  ```

- **S5d — FixedVarBirelationSet** — completed 2026-09-18; additive (green).
  Module: `relation/fixed_var.rs`. [dep: S4a]
  Implement the three methods across the fixed column and variable buffer/offsets.
  Verify row alignment and union incidence after addition and bulk removal.

  Added add, remove, and tracked_remove with the approved signatures and no new
  types, visibility changes, or payload bounds. Addition extends both factors,
  appends the second-factor boundary, and moves the payload into storage. Removal
  validates the entire request through Compaction before compacting the variable
  buffer, rewriting offsets, and moving the fixed column and payloads in survivor
  order. Incidence is rebuilt once over both final factors; empty removal returns
  identity without rebuilding. Plain remove delegates to tracked_remove. Neither
  operation reconstructs the set through new or clones payloads.

  Added 28 exact unit cases and two external properties. Cases cover either or
  both factors empty, adjacent empty variable rows, coinciding rows, references
  shared within/across factors, mixed node/edge factors, sparse ids, non-Clone
  payloads, stable row compaction, repeated/out-of-order removal ids, and invalid-id
  rejection before mutation. Properties compare mixed addition/replacement/removal
  sequences with independent rows, checking variable boundaries through public
  readers and incidence through direct union scans. A separate add/remove property
  checks restoration. Generated participants may reference nodes, edges, both,
  or neither.

  Verification passed: all 1,516 graph-core unit cases, 54 integration cases,
  all 160 properties with PROPTEST_CASES=256, all-target Clippy with proptest and
  warnings denied, Rust 1.87.0 all-target checking with proptest, and all six
  strict rustdoc configurations. Pinned nightly formatting and git diff --check
  passed. The full workspace gate remains at the S5 boundary.

  Added and ran 252 benchmarks in relation/fixed_var_rows, using 0/1/64/1,024
  initial rows, an arity-two EdgeId first factor, NodeId second factors of width
  0/4/32, usize payloads, and sparse/repeated references. Removal selections are
  empty, first, middle, last, alternating, and all rows. Setup cloning and final
  destruction stay outside timing; mutations include buffer/column movement,
  offset updates, compaction construction, and incidence rebuilding. On rustc
  1.96.0 (ac68faa20), aarch64-apple-darwin, ranges of measured means at 1,024 rows
  were:

  | Second-factor width | add | Single-row removal | Alternating-row removal | All-row removal |
  | ---: | ---: | ---: | ---: | ---: |
  | 0 | 9.46–16.9 µs | 11.8–19.2 µs | 11.2–14.7 µs | 6.93–7.09 µs |
  | 4 | 30.5–39.5 µs | 28.1–41.6 µs | 19.9–25.9 µs | 6.99–7.09 µs |
  | 32 | 127.6–161.4 µs | 127.6–218.1 µs | 69.8–83.5 µs | 7.08–7.67 µs |

  Removal ranges include both remove and tracked_remove. Empty removal ranged
  from 6.17–18.9 ns at 1,024 rows. These short synthetic measurements are
  descriptive, not workload claims or optimization thresholds. Reproduce with
  the maintained benchmark source and the command below, choosing a fresh
  baseline name for later comparisons. No scratch artifact is required.

  ```sh
  cargo bench --locked -p umol-graph-core --bench relation -- \
    'relation/fixed_var_rows/' --warm-up-time 0.1 --measurement-time 0.2 \
    --sample-size 20 --nresamples 10000 --noplot --save-baseline s5d-2026-09-18
  ```

- **S5e — VarVarBirelationSet** — completed 2026-09-18; additive (green).
  Module: `relation/var_var.rs`. [dep: S4a]
  Implement the three methods across both variable buffers and offset arrays.
  Cover independently varying factor lengths, including empty factors.

  Added add, remove, and tracked_remove with the approved signatures and no new
  types, visibility changes, or payload bounds. Addition extends both buffers,
  appends each factor's boundary, and moves the payload into storage. Removal
  validates the complete request through Compaction before compacting both buffers
  independently, rewriting their offsets, and moving payloads in survivor order.
  Incidence is rebuilt once over both final factors; empty removal returns identity
  without rebuilding. Plain remove delegates to tracked_remove. Neither operation
  reconstructs the set through new or clones payloads.

  Added 30 unit cases and two external properties. Cases cover unequal lengths,
  either or both factors empty, adjacent empty rows, coinciding rows, references
  shared within/across factors, mixed node/edge factors, sparse ids, non-Clone
  payloads, stable compaction, repeated/out-of-order removal ids, and invalid-id
  rejection before mutation. Properties compare mixed addition/replacement/removal
  sequences with independently edited rows, checking both sets of row boundaries
  through public readers and union incidence through direct scans. A separate
  add/remove property checks restoration of rows and incidence. Generated
  participants may reference nodes, edges, both, or neither.

  Verification passed: all 1,546 graph-core unit cases, 54 integration cases,
  and all 162 properties with PROPTEST_CASES=256. The full S5 gate below also passed.

  Added and ran 756 benchmarks in relation/var_var_rows: 0/1/64/1,024 initial rows,
  independently chosen factor widths 0/4/32, NodeId and EdgeId factors, usize
  payloads, and sparse/repeated references. Removal selections are empty, first,
  middle, last, alternating, and all rows. Setup cloning and final destruction
  stay outside timing; mutations include both buffer movements, offset updates,
  payload removal, compaction construction, and incidence rebuilding.

  The 234 cases at 1,024 rows were repeated after workspace verification finished
  to exclude its compiler/test load from this summary. On rustc 1.96.0 (ac68faa20),
  aarch64-apple-darwin, ranges of measured means were:

  | Factor widths | add | Single-row removal | Alternating-row removal | All-row removal |
  | --- | ---: | ---: | ---: | ---: |
  | 0, 0 | 3.11–3.42 µs | 7.8–8.09 µs | 8.78–8.87 µs | 7.01–7.11 µs |
  | 0, 4 | 21.8–24.8 µs | 23.5–32 µs | 17.2–19.6 µs | 7.07–7.19 µs |
  | 0, 32 | 69.3–119 µs | 73.1–98.7 µs | 42–59.1 µs | 7.09–7.21 µs |
  | 4, 0 | 23.6–26.5 µs | 26.2–32.3 µs | 18.5–20.6 µs | 7.1–7.23 µs |
  | 4, 4 | 46.1–47.1 µs | 38.7–52.4 µs | 25.9–32.7 µs | 7.16–7.5 µs |
  | 4, 32 | 95.1–153 µs | 97.2–112 µs | 54.7–68.9 µs | 7.11–7.17 µs |
  | 32, 0 | 113–167 µs | 118–139 µs | 64.2–80.7 µs | 7.05–7.22 µs |
  | 32, 4 | 129–163 µs | 131–151 µs | 71.6–87.1 µs | 7.13–7.25 µs |
  | 32, 32 | 178–308 µs | 178–277 µs | 95.5–125 µs | 7.12–7.36 µs |

  Removal ranges include both remove and tracked_remove. Empty removal ranged
  from 5.67–22.5 ns at 1,024 rows. These short synthetic measurements are
  descriptive, not workload claims or optimization thresholds. No affinity or
  thermal controls were applied. The maintained benchmark source and command
  below reproduce the cases; no scratch artifact is required.

  ```sh
  cargo bench --locked -p umol-graph-core --bench relation -- \
    'relation/var_var_rows/' --warm-up-time 0.1 --measurement-time 0.2 \
    --sample-size 20 --nresamples 10000 --noplot --save-baseline s5e-2026-09-18
  ```

  For the table alone, use the filter 'relation/var_var_rows/.*/rows=1024/' and
  baseline s5e-1024-2026-09-18. Choose fresh baseline names for later comparisons
  and run without concurrent workspace verification.

S5 gate: run the repository workspace gates and pinned CI MSRV check described
above, explicit graph-core properties with PROPTEST_CASES=256, strict rustdoc,
and the new benchmarks. Audit the public diff against these 15 methods and the
unchanged module/visibility boundaries. Record results here without depending on
scratch. Editor changes are not part of this gate or stage.

S5 gate completed 2026-09-18. The all-features workspace test run passed 33,236
cases with six ignored; workspace doctests passed five with four ignored.
The default workspace test run also passed. All-target/all-feature Clippy and
workspace rustdoc with warnings denied, the pinned Rust 1.87.0 all-target/all-feature
check, all six strict graph-core rustdoc configurations, pinned nightly formatting,
and git diff --check passed. The Python 3.13 extension was rebuilt; its suite
passed 1,545 cases with two skipped. The workspace reproduction commands above
apply; Python verification uses the python-build skill's activated environment,
maturin develop, and pytest -q umol-py/tests.

The public-surface audit against pre-S5 revision 3f8b776a2 found exactly the 15
approved methods. Removing those additions and ignoring comments/whitespace
leaves the five storage implementations identical to that revision. Existing
signatures, bodies, bounds, module boundaries, and visibility are unchanged.
The new methods use direct storage mutation, validate removal ids before mutation,
rebuild incidence once per nonempty mutation, and introduce no payload Clone
bounds. Independent test modules and external public-API properties remain in
place. Across S5, the additions comprise 128 unit cases, ten properties, and
1,428 maintained benchmark cases. Each subitem above retains its measurements
and reproduction instructions; disposable logs and Criterion data are not
required to use or verify the implementation.

The critical path is S0 → S1 → the five independent S2 type subitems → S3 → S4
→ the five independent S5 type subitems. All S2 and S5 methods are required,
including participant insertion/removal and whole-relation addition/removal.
No stage in this plan is optional. Incremental-index and local-edit
specializations are deferred follow-ups; the initial API works without them.
S6–S8 below extend this completed sequence with graph-core restoration.

### S6 — Restoration foundations and uncompaction lookup

S6–S8 implement the approved graph-core restoration surface and optimize the
existing uncompaction lookup. They do not rewire the editor, add edge-endpoint
replacement, change existing uncompact interfaces or semantics, or add
transaction journals. All subitems must finish green. The current public inventory
is Graph::restore and restore/restore_participants on all five relation-set types:
eleven methods returning (). S6a's error type was removed in the S7a contract
correction, a breaking change completed together with its caller migration.

- **S6a — RestoreError** — completed 2026-09-18; superseded by the S7a undo-contract correction.
  Modules: umol-graph-core/src/compact.rs and src/lib.rs. [dep: S5e]
  Initially added eleven error variants with Clone, Debug, PartialEq, Eq, Display,
  and Error, the crate-root export, and eleven diagnostic cases. The corrected
  contract above removes the type, export, and diagnostic tests. This record
  preserves the original stage's verification; no error vocabulary is required
  by subsequent restoration subitems.

  Verification: `cargo test --locked -p umol-graph-core --quiet` passed
  (1,557 unit cases, 53 integration cases, and the doctest target with no cases).
  `cargo clippy --locked -p umol-graph-core --all-targets -- -D warnings` and
  `RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps`
  passed. Pinned nightly formatting and `git diff --check` passed.
  Rust 1.87 checks are reserved for the final gate.

- **S6b — Maintained reconstruction benchmarks** — completed 2026-09-18;
  additive (green).
  Modules: umol-graph-core/benches/restore.rs (new) and Cargo.toml.
  [dep: S6a]
  Register a Criterion restore target covering Graph and all five relation shapes.
  Establish constructor-based reconstruction baselines using existing public
  APIs before native restoration methods exist. Keep reconstruction code local
  to the benchmark; it is neither production infrastructure nor test support.
  Check each fixture's complete expected rows/endpoints and incidence outside
  timing. Retain original entries independently of the reconstruction path.
  Cover identity, sparse/interleaved and complete removal, unordered saved entries,
  first/middle/last removed positions, sparse/shared references, and variable
  factor widths. Start with the existing synthetic sizes 1/64/1,024 and widths
  4/32; include empty fixtures and graph isolates, loops, and parallel edges.
  Separate row restoration, participant-reference restoration, and Graph
  reconstruction cases. Measure Graph with unique and shared CSR ownership.
  Exclude fixture capture, setup cloning, and final result destruction; include
  reconstruction and index/CSR maintenance. State remaining ownership/copy-cost
  differences when comparing the baseline with native operations.
  Run the benchmark fixtures and baseline measurements; record parameters,
  source revision or diff identity, case counts, and summarized results here.
  Keep the baseline runnable beside later native methods so comparison never
  depends on an old scratch directory or retained Criterion output.

- **S6c — Uncompaction lookup optimization** — completed 2026-09-18;
  additive (green).
  Modules: umol-graph-core/src/compact.rs and tests/property/compact.rs;
  unchanged benches/restore.rs for measurements. [dep: S6b]
  Replace the convergence loop in Compaction::try_uncompact with one binary
  search over the nondecreasing boundaries removed[i] - i. The number of
  boundaries at or below the result id is its source-id offset. Preserve
  Compaction's private representation, constructors, public signatures and
  trait bounds, inverse law, and domain checks: try_uncompact returns None
  outside the result domain; uncompact retains its existing panic behavior.
  GraphCompaction, participant translation, and Python wrappers continue to
  delegate through the existing APIs. No new types, caches, or caller changes.
  Add exact interleaved, contiguous-removal, and upper-id-boundary cases plus
  public-API exhaustive survivor-enumeration checks for every removal subset
  through source size 12. Retain the generated compaction law and run it with
  PROPTEST_CASES=256. Record the logarithmic lookup bound in rustdoc.
  Rerun the same 816 restoration benchmark cases under S6b's parameters,
  retaining its pre-fix record and reporting the new results separately.
  Gate: graph-core tests/doctests and compaction properties, strict graph-core
  Clippy/rustdoc, benchmark fixture checks and measurements, formatting, and
  diff check. Rust 1.87 remains part of the final gate only.

S6 gate: graph-core tests, doctests, and compaction properties; strict graph-core
Clippy/rustdoc; benchmark compilation, fixture checks, and baseline measurements;
formatting and diff check.

#### S6b reconstruction baseline — 2026-09-18

The maintained Criterion target is umol-graph-core/benches/restore.rs. Its
816 cases comprise 112 Graph cases and 704 relation cases: 352 row rebuilds
and 352 participant-reference rebuilds. Fixed and fixed/fixed each have 88
cases; variable, fixed/variable, and variable/variable each have 176. Helpers
are private to this benchmark target; it imports no test or benchmark modules
and adds no production symbols.

Fixture dimensions:

- Original relation counts and graph edge counts: 0, 1, 64, and 1,024.
  Fixed factors have width 4. Variable width limits are 4 and 32, with
  per-row lengths varying by up to two and selected empty factors.
- References are either distinct or shared/repeated within factors. Single
  fixed factors use NodeId, single variable factors use EdgeId, and birelations
  use NodeId in the first factor and EdgeId in the second. Payloads are usize.
- Removal patterns are identity, first, middle, last, every sixteenth position
  starting at one, interleaved odd positions, and all positions. Empty domains
  have only the identity case. Saved rows and edges are supplied in reverse
  id order. Relation participants use even original ids, so interleaved
  reference removal exercises inverse translation while retaining every row.
- Participant fixtures compact the original set before timing; expected
  surviving rows are independently selected from the original entries by
  reference coverage. The rows dimension names the original count, not the
  survivor count. Removing every reference can leave empty-factor rows in
  VarRelationSet; it leaves no rows in these fixed or birelation fixtures.
- Nonempty graph fixtures have edge_count + 2 nodes and include loops,
  parallel edges, and isolates. Graph cases remove both nodes and edges,
  with additional isolate-only and edge-only cases. Each runs with a newly
  constructed unique CSR and with a clone sharing the retained fixture's CSR.

Checks compare complete ordered entries, payloads, endpoints, and counts.
Relation incidence is independently collected from expected rows and checked
through the entire original node/edge reference domain, plus its upper bound.
Graph adjacency is independently collected from original endpoints, retaining
both occurrences of a loop and each parallel-edge id; comparison ignores only
neighbor ordering among equal nodes. Every fixture is checked outside timing,
including under Criterion's --test mode.

Timing uses iter_batched with BatchSize::LargeInput. Fixture capture, input
cloning, and final result destruction are excluded. Row rebuilds consume the
set with into_entries, scatter saved rows into original slots, fill the other
slots with surviving rows, and call new. Participant rebuilds consume entries,
expand their references with uncompact, and call new. Graph rebuilds materialize
original endpoint positions and call Graph::new, replacing the working graph.
Temporary allocation/destruction, consumed input disposal, and index/CSR
reconstruction are included. Unique-CSR disposal occurs inside timing; shared
CSR remains alive through the retained fixture.

**Benchmark interpretation for S6–S8.** Reconstruction helpers assume matching
inputs and can panic on manipulated data; lower timings do not make them acceptable
replacements. Use them to assess the cost of the implemented restoration algorithm
and whether further optimization is worthwhile. Compare candidate replacements
under the same no-panic contract, ownership setup, and timing boundaries. These
synthetic fixtures do not establish molecular workload frequencies or performance.
Each subitem records its implementation choice and specific findings below.

Clarification recorded 2026-09-19: timing work must answer a concrete implementation
question. A reconstruction baseline with weaker semantics cannot decide between
viable restoration implementations, and more precise measurements of that gap do
not justify additional runs. Retain existing evidence; further comparisons require
alternatives satisfying the same contract and a decision the results can change.
Benchmark fixture correctness checks remain part of verification.

The baselines omit identity fast paths. Variable into_entries materializes per-row
Vecs, and Graph::new copies from the temporary endpoint buffer; native methods may
avoid those costs. The graph isolates fixture removes a trailing degree-zero node,
not a single bonded atom.

Run identity: parent revision 4682b2b5f9f38aac4f024148a3634034469d7624 plus
the new restore target and its Cargo registration. SHA-256 of the measured
benchmark source:
`21fd71d800beeaacc2a39d46599f403da9a74361cb686bb5d15bc73126c54cb1`.
Cargo.toml SHA-256:
`0dc7641128ade072cb11240549827561e5441ee46e28f2a5c4c13c8dc1fc6915`.
The run used rustc 1.96.0 (ac68faa20, 2026-05-25), arm64 macOS, the workspace
bench profile, and Criterion 0.8.1. Commands:

```sh
cargo bench --locked -p umol-graph-core --bench restore -- --test
cargo bench --locked -p umol-graph-core --bench restore -- \
  --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
  --nresamples 10000 --noplot --save-baseline s6b-2026-09-18
```

All 816 cases completed with 20 samples each. The eight cases exceeding the
requested 0.2-second sampling window still collected all 20 samples; Criterion
extended their estimated windows to 0.215–0.592 seconds. These short runs are
baselines for fresh comparisons, not precise performance guarantees.

Selected Criterion mean estimates in microseconds, at 1,024 original rows
with interleaved removal. Each pair is sparse/shared references:

| Shape | Width limit | Row rebuild (µs) | Participant rebuild (µs) |
| --- | ---: | ---: | ---: |
| FixedRelationSet | 4 | 25.42 / 29.11 | 1,383.89 / 585.93 |
| VarRelationSet | 4 | 33.76 / 35.97 | 930.40 / 293.62 |
| VarRelationSet | 32 | 128.19 / 94.93 | 14,413.24 / 4,877.02 |
| FixedFixedBirelationSet | 4 | 37.26 / 47.83 | 2,754.40 / 1,159.19 |
| FixedVarBirelationSet | 4 | 50.15 / 57.51 | 2,304.38 / 1,032.37 |
| FixedVarBirelationSet | 32 | 146.57 / 128.81 | 16,360.48 / 5,685.13 |
| VarVarBirelationSet | 4 | 70.22 / 75.16 | 1,864.34 / 588.71 |
| VarVarBirelationSet | 32 | 277.04 / 220.43 | 29,390.13 / 9,868.12 |

Across the same sparse-reference cases, row rebuild means span 0.201–0.800 µs
at one row and 2.179–15.899 µs at 64 rows; participant rebuild means span
0.184–2.642 µs and 21.788–943.281 µs respectively. Interleaved participant
expansion is substantially costlier than row reconstruction at the larger
sizes. This baseline invokes the existing Compaction::uncompact separately
for every reference; that method repeatedly searches the removed-id list
until the source id stabilizes. S6b does not change that implementation.

Graph mean estimates in microseconds at 1,024 original edges:

| Reference pattern | CSR ownership | Identity | Interleaved | All removed |
| --- | --- | ---: | ---: | ---: |
| Sparse | Unique | 13.61 | 206.42 | 9.46 |
| Sparse | Shared | 13.46 | 224.73 | 9.49 |
| Shared | Unique | 16.94 | 126.07 | 12.65 |
| Shared | Shared | 16.85 | 126.69 | 12.73 |

Verification passed: the optimized benchmark build and all 816 fixture checks;
1,557 graph-core unit cases, 53 integration cases, and the doctest target
(no cases); strict all-target graph-core Clippy and rustdoc; pinned nightly
formatting; and git diff --check. In addition to the benchmark commands above:

```sh
cargo test --locked -p umol-graph-core --quiet
cargo clippy --locked -p umol-graph-core --all-targets -- -D warnings
RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
cargo +nightly-2026-06-04 fmt --all --check
git diff --check
```

The original S6a–S6b gate passed; the separately approved S6c extends S6 below.
Rust 1.87 checks remain reserved for the final gate.
The maintained benchmark and this record retain the fixtures, reproduction
commands, run identity, and summarized results. scratch and Criterion output
are disposable. For subsequent comparisons, rerun the constructor baseline
alongside the native methods under the same parameters.

#### S6c uncompaction lookup and remeasurement — 2026-09-18

Compaction::try_uncompact now finds the source-id offset with one binary
search and no allocation. For sorted, distinct removed ids r[i], r[i] - i
counts surviving source ids before that removal. These counts are nondecreasing,
including equal counts across a contiguous removed run. The removals that precede
result id p are exactly those with r[i] - i <= p. Adding their number to p
therefore recovers its source id. The existing result-domain check still runs
before the search. Representation, trait bounds, constructor behavior, inverse
semantics, and None/panic behavior are unchanged; all existing wrappers delegate
to the optimized lookup.

Six new exact cases cover a large alternating removal, long prefix/middle/suffix
runs, and ids near the u32 boundary. The external compaction suite additionally
checks all 8,191 removal subsets through source size 12 against independent
survivor enumeration, checking both inverse entry points and two out-of-result
ids. The existing generated compaction/id/value/correspondence law is retained.
Both the added cases and the exhaustive check passed against the old algorithm
before the replacement and pass against the new algorithm.

The benchmark source, fixtures, 816 case ids, compiler, profile, and parameters
are unchanged from S6b. This run uses the same parent revision and S6b additions,
plus S6c's compact.rs change and tests. Measured compact.rs SHA-256:
`d84d0d6d30240db83714df2fae5470f7d47d71b4e2a4941cdd53ddc3622780d4`.
S6b's results remain the pre-optimization record; S6c is a separate baseline:

```sh
cargo bench --locked -p umol-graph-core --bench restore -- --test
cargo bench --locked -p umol-graph-core --bench restore -- \
  --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
  --nresamples 10000 --noplot --save-baseline s6c-2026-09-18
```

All 816 cases completed with 20 samples each. Selected Criterion mean
estimates at 1,024 original rows with interleaved removal:

| Shape | Width limit | Sparse before → after (µs) | Shared before → after (µs) |
| --- | ---: | ---: | ---: |
| FixedRelationSet | 4 | 1,383.89 → 90.56 | 585.93 → 98.18 |
| VarRelationSet | 4 | 930.40 → 81.51 | 293.62 → 89.10 |
| VarRelationSet | 32 | 14,413.24 → 765.09 | 4,877.02 → 790.25 |
| FixedFixedBirelationSet | 4 | 2,754.40 → 168.97 | 1,159.19 → 187.17 |
| FixedVarBirelationSet | 4 | 2,304.38 → 165.45 | 1,032.37 → 181.25 |
| FixedVarBirelationSet | 32 | 16,360.48 → 878.29 | 5,685.13 → 922.03 |
| VarVarBirelationSet | 4 | 1,864.34 → 160.36 | 588.71 → 176.42 |
| VarVarBirelationSet | 32 | 29,390.13 → 1,566.67 | 9,868.12 → 1,729.25 |

These participant cases improved by 3.3–18.8× in this run.
Graph reconstruction with interleaved removal at 1,024 original edges:

| Reference pattern | CSR ownership | Before (µs) | After (µs) |
| --- | --- | ---: | ---: |
| Sparse | Unique | 206.42 | 17.26 |
| Sparse | Shared | 224.73 | 17.36 |
| Shared | Unique | 126.07 | 21.65 |
| Shared | Shared | 126.69 | 21.92 |

The median after/before mean ratio across the 352 unchanged row-rebuild cases
was 0.999 (range 0.872–1.126). These controls do not use uncompaction.
The short sequential runs retain timing variation; these ratios describe
this synthetic experiment, not guaranteed application speedups. Native
restoration comparisons should use the S6c constructor baseline, with fresh
baseline/native measurements under matching conditions.

The complete comparison also found three slower VarVarBirelationSet cases
with shared references and width limit 32. A focused rerun used 0.5-second
warmup, 2-second measurement, and 50 samples; it did not reproduce those
regressions. Both the initial S6c values and the rerun are retained below
(Criterion mean estimates in microseconds):

| Rows / removal | S6b | S6c full run | S6c focused rerun |
| --- | ---: | ---: | ---: |
| 64 / sparse | 53.09 | 111.80 | 51.62 |
| 1,024 / middle | 325.13 | 384.26 | 323.36 |
| 1,024 / last | 326.10 | 364.15 | 326.12 |

```sh
cargo bench --locked -p umol-graph-core --bench restore -- \
  'restore/var_var/participants_rebuild/rows=(64/width=32/shared/sparse|1024/width=32/shared/(middle|last))$' \
  --warm-up-time 0.5 --measurement-time 2 --sample-size 50 \
  --nresamples 10000 --noplot --save-baseline s6c-focused-2026-09-18
```

In the full S6c run, four sampling windows were extended to 0.264–0.361
seconds; every case still collected all 20 samples. The saved runs and tables
preserve the before/after distinction without replacing the S6b record.

Verification passed: 1,563 unit cases, 53 integration cases, the doctest target
(no cases), the generated compaction law with PROPTEST_CASES=256 and the
exhaustive check, all 816 optimized benchmark fixture checks, strict all-target
graph-core Clippy with proptest enabled, strict rustdoc, pinned nightly
formatting, and git diff --check. Commands, in addition to the benchmark run:

```sh
cargo test --locked -p umol-graph-core --quiet
PROPTEST_CASES=256 cargo test --locked -p umol-graph-core \
  --features proptest --test property compact:: --quiet
cargo clippy --locked -p umol-graph-core --all-targets \
  --features proptest -- -D warnings
RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
cargo +nightly-2026-06-04 fmt --all --check
git diff --check
```

The extended S6 gate is complete. Rust 1.87 remains reserved for the final
gate. Run identities, commands, results, and verification are recorded here;
scratch and Criterion output remain disposable. S7a is next.

### S7 — Graph and relation restoration

Each subitem supplies its own unit cases, external public-API properties, rustdoc,
and native benchmark cases against the reconstruction benchmarks introduced in
S6b and remeasured in S6c. Unit modules remain independent, with explicit imports and no cross-imported test
fixtures or constructor helpers. Preserve existing test/module organization and
visibility. Keep property scenarios and independent row/endpoint references in
umol-graph-core/tests/property/restore.rs (new), registered in tests/property.rs;
do not import from the graph or relation property modules.

For every method, document its inputs, id spaces, undo provenance, freedom from
panics on manipulated input, and semantic laws. Link the public laws from
property-module docs using public exports. Follow the established method/test order in each storage module.

- **S7a — Graph::restore** — completed 2026-09-18; additive implementation with
  a breaking undo-contract correction (red→green).
  Modules: src/graph.rs, src/compact.rs, src/lib.rs, tests/property/restore.rs,
  tests/property.rs, and benches/restore.rs. [dep: S6c]
  Restore original node/edge ids, translate surviving endpoints, and rebuild CSR
  through Graph's existing internal construction. Preserve loops, parallel edges,
  and untouched shared graph values. Cases cover isolated nodes, node-only and
  edge-only restoration, identity, complete removal, unordered entries, and
  manipulated counts, entries, and endpoints without panics. Properties compare
  full topology and adjacency to an independent endpoint model and exercise
  removal/restoration roundtrips. Add and measure native Graph benchmark cases.

  **Initial checked implementation (superseded by the correction below).**

  Added Graph::restore with the approved signature and validation order. It
  checks counts, scans saved ids in input order using a removed-id membership
  search and a seen vector, reports the lowest missing edge, and checks saved
  endpoints against the original node count. All checks precede mutation.
  Identity restoration returns without replacing the CSR; other cases expand
  surviving endpoints, place saved edges at their original ids, and call the
  existing internal CSR builder. The only new public symbol is Graph::restore;
  no visibility changes, supporting types, or editor changes were introduced.

  Added 39 unit cases: five restoration fixtures under both unique and shared
  ownership, three identities, and thirteen error/precedence fixtures under
  both ownership conditions. Cases include isolated nodes, node-only and
  edge-only restoration, complete removal, unordered saved entries, endpoint
  normalization, loops, parallel edges, and all six applicable error variants.
  Identity and error cases also check that the CSR allocation is retained.
  Three external public-API properties cover an independently enumerated
  endpoint/adjacency model, cascading-removal roundtrips with retained shared
  graphs, and malformed-input atomicity. Their domain is up to 16 nodes and
  40 edges; expected compacted endpoints are derived by survivor enumeration,
  independently of the production removal and translation paths. The property
  module imports no other test module. Rustdoc states the contract and laws,
  links the property location, and demonstrates restoring an isolated node.

  Added 112 native graph benchmark cases beside the 112 reconstruction cases.
  Both paths pass the same independent endpoint/adjacency fixture checks.
  The complete restore benchmark now has 928 cases; all fixture checks passed.
  The timed S7a run covers only the 224 graph cases, with fresh reconstruction
  measurements using S6c's optimized uncompaction. Sizes, removal patterns,
  unique/shared CSR setup, and excluded setup/output destruction match S6b/S6c.
  Native timings include input validation. Nonidentity restoration still
  allocates an endpoint buffer and rebuilds CSR; storage ownership by itself
  does not remove those costs. The reconstruction baseline omits validation.

  Run identity: parent revision c08091e67fbc99ea7765f93631a51a13c5c507dd plus
  this S7a diff. SHA-256 of measured src/graph.rs:
  `193175b6fd2553e7879f6116e81fd0184b2ec1d872ad6507c7ced81d16e11a3a`;
  benches/restore.rs:
  `f9b37f30fc8cfeabe4eedcfac7f5208b87e6e12ffd6e2caef16e477c38407ed6`.
  Compiler: rustc 1.96.0 (ac68faa20, 2026-05-25), arm64 macOS, workspace bench
  profile, Criterion 0.8.1. Reproduction commands:

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- --test
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/graph/' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7a-2026-09-18
  ```

  All 224 graph cases completed with 20 samples each. Selected Criterion mean
  estimates at 1,024 original edges are shown as reconstruction / native
  restoration in microseconds:

  | References | Removal | Unique CSR (µs) | Shared CSR (µs) |
  | --- | --- | ---: | ---: |
  | Sparse | identity | 12.285 / 0.017 | 12.049 / 0.004 |
  | Sparse | isolates | 12.899 / 11.167 | 12.872 / 11.152 |
  | Sparse | interleaved | 17.888 / 22.722 | 17.940 / 22.557 |
  | Sparse | edges_only | 9.335 / 15.810 | 9.172 / 15.683 |
  | Sparse | all | 9.301 / 15.819 | 9.319 / 15.827 |
  | Shared | identity | 15.702 / 0.018 | 15.470 / 0.004 |
  | Shared | isolates | 16.347 / 14.792 | 16.304 / 14.671 |
  | Shared | interleaved | 22.112 / 26.431 | 21.704 / 26.513 |
  | Shared | edges_only | 12.774 / 19.340 | 12.763 / 19.549 |
  | Shared | all | 12.693 / 19.294 | 12.677 / 19.284 |

  Identity restoration avoids CSR construction. At 1,024 edges, native
  interleaved restoration is about 1.20–1.27 times the reconstruction mean;
  complete edge restoration is about 1.51–1.71 times the reconstruction mean.
  These timings include validation only on the native path and do not establish
  that storage-owned restoration is universally faster. Isolate-only native
  means were modestly lower in this run. The short synthetic measurements
  establish a maintained comparison, not application workload performance.

  Verification passed: 1,602 graph-core unit cases, 53 integration cases, and
  one doctest; all three restoration properties with PROPTEST_CASES=256;
  strict all-target Clippy with proptest enabled; strict rustdoc; all 928
  benchmark fixture checks; pinned nightly formatting; and git diff --check.
  Commands, in addition to the benchmark commands above:

  ```sh
  cargo test --locked -p umol-graph-core --quiet
  PROPTEST_CASES=256 cargo test --locked -p umol-graph-core \
    --features proptest --test property restore:: --quiet
  cargo clippy --locked -p umol-graph-core --all-targets \
    --features proptest -- -D warnings
  RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
  cargo +nightly-2026-06-04 fmt --all --check
  git diff --check
  ```

  These initial results describe the superseded checked implementation. The
  source identities and commands retain that evidence independently of scratch.

  **Undo-contract correction, 2026-09-18 — breaking (red→green).**

  Removed RestoreError, its export and diagnostic tests, and changed Graph::restore
  and its callers to return (). Removed count equality, removed-id membership,
  duplicate detection, missing-entry reporting, and the preliminary validation pass.
  Survivor translation and saved-entry placement use local panic guards. CSR size
  bounds protect arithmetic and allocation capacity; an empty node domain cannot
  reach adjacency construction with edges. Nonidentity cases still build an endpoint
  buffer and reuse the CSR builder. No editor or relation implementation changes.

  Valid-input topology, shared-storage, and identity coverage is retained. The unit
  malformed-input cases now check only freedom from panics, including invalid survivor
  domains, missing edges with no nodes, and oversized counts. Generated malformed
  inputs independently vary the current graph, compaction, and saved entries; they
  impose no result or unchanged-state requirement. Tests remain independent.

  Verification passed: 51 focused restore unit cases (13 valid-input/identity and
  38 malformed-input cases), three restoration properties with PROPTEST_CASES=256,
  all 1,603 graph-core unit cases, 53 integration cases, and one doctest. Strict
  all-target Clippy with proptest, strict rustdoc, all 928 benchmark fixture checks,
  pinned-nightly formatting, and diff checks passed. Use the verification commands
  above; the focused unit filter is test_graph_restore. No Rust 1.87 check was run;
  it remains reserved for the final gate.

  Remeasured all 224 graph cases (112 native, 112 reconstruction) with the same
  ownership, fixtures, sample settings, and timing boundaries. No sampling warnings
  were emitted. Command:

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/graph/' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7a-undo-2026-09-18
  ```

  Host/compiler remain arm64 macOS and rustc 1.96.0 (ac68faa20 2026-05-25).
  Measured src/graph.rs SHA-256:
  `bb91625761f64ccb64353db0ed60779d878cfe16d262c9bfcb944af8673c804f`;
  measured benches/restore.rs:
  `512749dd8189015847780cb765dd2f7cb1f9bef43150b633bb0e5e5c02d21ca9`.
  The benchmark module comment was subsequently corrected from validation to panic
  guards; the measured code is unchanged. Parent revision is the same as the initial
  S7a run above, with this correction applied.

  Mean times in microseconds at 1,024 original edges:

  | References | Removal | Native, unique CSR | Native, shared CSR | Rebuild, unique CSR | Rebuild, shared CSR |
  | --- | --- | ---: | ---: | ---: | ---: |
  | sparse | identity | 0.017 | 0.003 | 12.516 | 12.501 |
  | sparse | isolates | 11.814 | 11.586 | 13.373 | 13.381 |
  | sparse | interleaved | 17.084 | 17.271 | 17.146 | 17.300 |
  | sparse | edges_only | 9.522 | 10.121 | 9.267 | 9.423 |
  | sparse | all | 9.830 | 9.976 | 9.752 | 9.551 |
  | shared | identity | 0.018 | 0.003 | 15.825 | 15.481 |
  | shared | isolates | 15.060 | 14.678 | 16.377 | 16.407 |
  | shared | interleaved | 21.598 | 21.527 | 21.728 | 21.571 |
  | shared | edges_only | 12.961 | 13.114 | 12.625 | 12.794 |
  | shared | all | 13.268 | 13.145 | 12.905 | 12.956 |

  Relative to the checked S7a implementation above, native interleaved means fell
  18–25%, edge-only means 33–40%, and complete-removal means 31–38%. Interleaved
  restoration is within 1% of the fresh reconstruction baseline. Native edge-only
  and complete-removal cases still cost about 1–7% more than reconstruction; bounds
  guards remain in the timed path. Isolate-only native means changed by 0–6% and
  remain below their fresh reconstruction baselines. Across all 112 unchanged
  reconstruction controls, the median new/old ratio is 0.997 (range 0.852–1.183),
  showing the noise in these short synthetic runs. These measurements do not
  establish application workload performance or a universal speedup.

  Retain Graph's guarded full reconstruction for this stage. Identity avoids the
  rebuild; the other cases still allocate endpoints and reconstruct adjacency.
  Storage-specific optimization can follow without changing the API.

  Final review confirms the eleven-method design inventory, removal of the error
  type, unchanged existing compaction APIs, and independent test modules. The
  contract, benchmark summary, source identities, and reproduction commands are
  retained here; scratch outputs are disposable. S7b is next.

- **S7b — FixedRelationSet restoration** — completed 2026-09-18; additive (green).
  Modules: src/relation/fixed.rs, src/relation/tests/fixed.rs,
  tests/property/restore.rs, and benches/restore.rs. [dep: S6c]
  Implement restore and restore_participants for fixed rows, including arity zero.
  Guard destination access, missing payloads, and participant inverse translation
  against panics without a preliminary validation pass. Restore row columns and
  rebuild incidence without adding a payload Clone bound. Add the common
  relation verification below and measure both native operations.

  Added exactly FixedRelationSet::restore(&Compaction<RelationId>,
  Vec<(RelationId, [P; N], D)>) and restore_participants(&GraphCompaction), both
  returning (). No Clone bound on D, new error, public support type, participant
  trait method, visibility change, or editor integration was introduced. Rustdoc
  states matching-history roundtrips, preservation laws, and freedom from panics
  for manipulated undo data with conforming participants. Neither method reports
  errors or promises an unchanged result for manipulated input; no Errors or Panics
  sections assert the superseded failure contract.

  Row restoration appends saved rows to the existing columns and places them by
  original id with a temporary destination array. A linear scan of sorted removed
  ids computes survivor destinations. Every swap settles one destination; an
  out-of-range or already settled destination stops that placement on manipulated
  input. Missing rows require neither placeholder payloads nor unwrapping. Capacity
  is based on actual supplied rows, not the claimed source count. Participant
  restoration edits participant arrays in place and leaves the payload column in
  place. Reference guards precede each inverse translation, without a preliminary
  whole-set validation pass. Both operations rebuild the full incidence index;
  neither supplies an incremental incidence algorithm.

  Added 30 unit cases covering unordered/interleaved rows, complete removal,
  coincident rows and repeated participants, changed surviving payloads, empty sets,
  zero arity, identities, non-Clone payloads, and manipulated counts/ids/references.
  Structured participants include node, edge, both, and neither reference plus
  non-reference labels. Six new public-API properties independently model row
  restoration and inverse references, check producer roundtrips, and require only
  freedom from panics for malformed inputs. Saved-row permutations use generated
  sort keys. Valid row domains have up to 32 rows; inverse-reference domains have
  up to eight nodes and edges. Unit and property modules remain independent.

  Verification passed: all 1,633 graph-core unit cases, 53 integration cases, one
  doctest, and all nine restoration properties with PROPTEST_CASES=256 (six new).
  Strict all-target Clippy with proptest, strict rustdoc, all 1,104 benchmark
  fixture checks, formatting, and diff checks passed.
  Rust 1.87 remains reserved for the final gate.

  ```sh
  cargo test --locked -p umol-graph-core test_fixed_relation_set_restore --quiet
  cargo test --locked -p umol-graph-core --quiet
  PROPTEST_CASES=256 cargo test --locked -p umol-graph-core \
    --features proptest --test property restore:: --quiet
  cargo clippy --locked -p umol-graph-core --all-targets \
    --features proptest -- -D warnings
  RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
  cargo bench --locked -p umol-graph-core --bench restore -- --test
  cargo +nightly-2026-06-04 fmt --all --check
  git diff --check
  ```

  The benchmark now distinguishes cloning the compacted set (which loses spare
  column capacity) from removing rows from a fresh original set during setup (which
  retains it). Both native and reconstruction paths receive the same setup in each
  comparison; removal is excluded from timing. Fixed fixtures use four NodeId
  participants, usize payloads, 0/1/64/1,024 rows, sparse/shared references, and
  identity/first/middle/last/sparse/interleaved/all removal patterns. Whole-row
  restoration has 176 cases across two algorithms and two capacity setups;
  participant restoration has 88 cases. These synthetic cases do not establish
  molecular workload frequencies or costs for large payloads.

  Initial measurement exposed repeated binary uncompaction searches while forming
  the row destination array. At 1,024 rows with retained capacity, scattered and
  interleaved native restoration took 30.369/30.846 µs for sparse references and
  36.045/36.328 µs for shared references, versus reconstruction at
  23.510/23.694 and 27.684/28.361 µs. Replaced those repeated lookups with the linear
  scan, then remeasured all 176 row cases. The 88 participant cases were unchanged.
  Across the 88 unchanged row-reconstruction controls, the median second/first
  mean ratio was 0.998 (range 0.869–1.103); short-run noise limits small differences.

  Source identity: parent 8cc8f489fa0374cb117b59e85b4d9604dca29f26 plus this S7b diff.
  Initial src/relation/fixed.rs SHA-256:
  `9a3a9dea3644e4cc4a70b64e17579ff39750a1faa1e468f68f8a18809d57df8d`;
  final linear-scan implementation:
  `61e22eadc4ba0977c8d90346fc53018f848b2a744a886d7cd3799eee39e0892d`;
  measured benches/restore.rs:
  `c49b473b27a965f4225940a4a42b513f95c4b23b533db515bfe115c5d201df22`.
  Host/compiler: arm64 macOS, rustc 1.96.0 (ac68faa20 2026-05-25), workspace bench
  profile, Criterion 0.8.1. Initial and final-row commands:

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/fixed/' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7b-2026-09-18
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/fixed/rows_' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7b-linear-2026-09-18
  ```

  Selected means in microseconds at 1,024 original rows, shown as reconstruction /
  native. Row values are from the linear-scan run; participant values are from the
  unchanged initial run. A participant case may retain fewer than 1,024 rows:
  shared/first and both all cases retain no rows.

  | References | Removal | Rows, cloned capacity | Rows, retained capacity | Participants |
  | --- | --- | ---: | ---: | ---: |
  | sparse | identity | 23.869 / 0.014 | 23.787 / 0.014 | 27.736 / 0.014 |
  | sparse | first | 23.680 / 26.816 | 23.788 / 26.772 | 28.666 / 27.965 |
  | sparse | middle | 23.708 / 24.561 | 23.773 / 23.845 | 28.434 / 27.918 |
  | sparse | last | 24.682 / 23.751 | 24.935 / 22.809 | 30.758 / 30.839 |
  | sparse | sparse | 24.280 / 27.110 | 23.710 / 26.546 | 72.356 / 68.663 |
  | sparse | interleaved | 24.250 / 25.927 | 24.192 / 25.305 | 93.138 / 101.470 |
  | sparse | all | 23.923 / 23.748 | 24.111 / 23.665 | 0.027 / 0.013 |
  | shared | identity | 27.209 / 0.013 | 27.494 / 0.013 | 30.703 / 0.013 |
  | shared | first | 27.569 / 32.218 | 27.815 / 32.356 | 0.023 / 0.013 |
  | shared | middle | 27.663 / 31.107 | 27.749 / 29.475 | 32.431 / 31.610 |
  | shared | last | 27.402 / 27.982 | 28.110 / 28.129 | 32.067 / 31.630 |
  | shared | sparse | 28.572 / 32.581 | 27.659 / 31.590 | 73.540 / 73.124 |
  | shared | interleaved | 27.790 / 31.417 | 27.954 / 31.082 | 102.201 / 98.611 |
  | shared | all | 27.737 / 29.377 | 27.774 / 29.514 | 0.030 / 0.014 |

  A longer run checked the remaining row overhead and the initial sparse/interleaved
  participant regression. All 12 selected cases completed with 40 samples, a
  0.3-second warm-up, and one-second measurement; no sampling warnings occurred in
  any S7b run. Command:

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- \
    'restore/fixed/(rows_(rebuild|restore)_retained/rows=1024/width=4/(sparse|shared)/(first|interleaved)|participants_(rebuild|restore)/rows=1024/width=4/(sparse|shared)/interleaved)$' \
    --warm-up-time 0.3 --measurement-time 1 --sample-size 40 \
    --nresamples 20000 --noplot --save-baseline s7b-focused-2026-09-18
  ```

  | Operation | References | Removal | Reconstruction (µs) | Native (µs) |
  | --- | --- | --- | ---: | ---: |
  | rows, retained capacity | sparse | first | 23.777 | 26.331 |
  | rows, retained capacity | shared | first | 27.851 | 32.334 |
  | rows, retained capacity | sparse | interleaved | 23.779 | 25.651 |
  | rows, retained capacity | shared | interleaved | 28.024 | 31.198 |
  | participants | sparse | interleaved | 89.557 | 86.191 |
  | participants | shared | interleaved | 96.643 | 95.096 |

  Retain the linear destination scan: it reduced scattered/interleaved row costs
  without changing the API. In-place row restoration still costs about 8–16% more
  in the focused retained-capacity cases; capacity reuse did not eliminate that
  gap. Participant restoration is about 2–4% faster, and its initial 9%
  sparse/interleaved regression did not reproduce. Keep the current column reuse
  and full incidence rebuild for S7b; specialized row insertion and incremental
  incidence remain future internal optimizations. These runs do not isolate row
  swapping, guards, or incidence rebuilding as the cause of the remaining gap.

  Final API/diff review confirmed only the two approved public methods, preserved
  existing mutation and compaction contracts, no test-module coupling, and explicit
  no-panic properties. Measurements, source identities, commands, and the decision
  are retained here so scratch outputs are disposable. S7c is next.

- **S7c — VarRelationSet restoration** — completed 2026-09-18; additive (green).
  Modules: src/relation/var.rs, src/relation/tests/var.rs,
  tests/property/restore.rs, and benches/restore.rs. [dep: S6c]
  Implement both methods, restoring packed participant storage and offsets for
  heterogeneous row lengths, including empty rows. Participant-reference
  restoration preserves all row lengths and positions. Add the common relation
  verification and measure both operations.

  Implemented the two approved public methods: restore takes
  &Compaction<RelationId> and Vec<(RelationId, Vec<P>, D)>;
  restore_participants takes &GraphCompaction. Both return (), require no D: Clone,
  and retain the undo/no-panic contract. No new public type, constructor, error,
  visibility change, or shared test support was introduced.

  Whole-row restoration indexes moved payloads and participant ranges by original
  relation id, using a linear scan of removed ids for survivor placement. It
  reuses the payload/offset columns and builds one replacement packed participant
  buffer, without materializing a Vec for each surviving row. Allocations use
  actual live/saved sizes, not an untrusted source count. Local capacity, offset,
  and indexing guards prevent malformed input from panicking; they do not validate
  the undo history. Empty and heterogeneous rows retain their boundaries.

  Participant restoration edits the existing packed buffer, guarding exposed
  references immediately before inverse translation; row offsets and payloads
  stay in place. Both methods rebuild incidence. Whole-row restoration temporarily
  holds old and new packed buffers plus the row-range array; saved participants
  pass through the old buffer before their final copy. It therefore still performs
  full participant/index work even for one restored row. This implementation does
  not claim incremental restoration or allocation-free row insertion.

  Added 32 unit cases and six public-API properties. Coverage includes unordered
  saved rows, interleaved and complete removal, empty rows/sets, heterogeneous
  lengths, repeated participants, coincident rows, changed survivor payloads,
  identities, non-Clone payloads, and malformed undo inputs. Structured participants
  carry node, edge, both, or neither reference plus labels. Independent row and
  incidence scans supplement producer roundtrips; generated rows contain zero to
  eight participants and sets up to 32 rows. Malformed cases assert only freedom
  from panics. Test modules do not import from each other.

  Verification passed: 1,665 graph-core unit cases, 53 integration cases, one
  doctest, all 15 restoration properties at PROPTEST_CASES=256, strict all-target
  Clippy with proptest, strict rustdoc, and all 1,456 benchmark fixture checks
  (528 VarRelationSet cases). Formatting and diff checks passed. Rust 1.87 remains
  reserved for the final gate.

  ```sh
  cargo test --locked -p umol-graph-core test_var_relation_set_restore --quiet
  cargo test --locked -p umol-graph-core --quiet
  PROPTEST_CASES=256 cargo test --locked -p umol-graph-core \
    --features proptest --test property restore:: --quiet
  cargo clippy --locked -p umol-graph-core --all-targets \
    --features proptest -- -D warnings
  RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
  cargo bench --locked -p umol-graph-core --bench restore -- --test
  cargo +nightly-2026-06-04 fmt --all --check
  git diff --check
  ```

  Benchmarks cover 0/1/64/1,024 rows, maximum widths 4/32, EdgeId participants,
  usize payloads, sparse/shared references, and the seven existing removal patterns.
  Every eighth row is empty; other lengths are width minus row index modulo three.
  Both row algorithms receive matched fresh-clone or retained-capacity setup;
  removal and input cloning stay outside timing. Participant cases use the same
  compacted set for both algorithms. The shared S6–S8 benchmark interpretation
  above applies.

  Measured on arm64 macOS 15.7.3 with rustc 1.96.0 (ac68faa20 2026-05-25),
  based on commit 28a8782545e040bed76e9914c8742e1cdbcc8eba plus this S7c diff.
  SHA-256 identities of the measured sources:

  - src/relation/var.rs:
    cdfa2b3a97733a95448ae1cd66527138522e5483a6c8db8a9d7a132891328eed
  - benches/restore.rs:
    d20b3884b18c423698cb7d0fc3aa9ed8f565c3cbe129dcbb94295296a195e9df

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/var/' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7c-2026-09-18
  ```

  All 528 VarRelationSet benchmark cases completed. Selected mean times below
  are microseconds, reconstruction → native restoration, at 1,024 original rows.
  Row measurements use retained-capacity setup.

  | Width | References | Restore first row | Restore interleaved rows | Restore all rows |
  | --- | --- | --- | --- | --- |
  | 4 | sparse | 38.22 → 21.45 | 33.84 → 25.91 | 28.58 → 31.25 |
  | 4 | shared | 42.19 → 24.76 | 38.19 → 30.23 | 32.27 → 34.50 |
  | 32 | sparse | 149.65 → 84.24 | 155.98 → 97.53 | 140.58 → 104.37 |
  | 32 | shared | 132.86 → 68.76 | 132.15 → 77.09 | 113.87 → 87.10 |

  | Width | References | Expand references after first removal | Expand references after interleaved removal |
  | --- | --- | --- | --- |
  | 4 | sparse | 42.25 → 18.48 | 80.95 → 58.27 |
  | 4 | shared | 1.04 → 0.24 | 88.31 → 68.58 |
  | 32 | sparse | 214.92 → 120.17 | 795.28 → 710.88 |
  | 32 | shared | 1.04 → 0.23 | 822.94 → 780.12 |

  Shared-reference first removal drops every nonempty row; 128 empty rows survive,
  so its sub-microsecond native result does not measure reference translation.
  Sparse first and both interleaved reference cases retain nonempty rows. Complete
  graph-reference removal also leaves only the empty rows.

  The short run found a 7–9% complete-row-removal regression at width 4; the width
  32 cases instead improved. A longer repeat isolates these four retained-capacity
  fixtures (eight measurements):

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- \
    'restore/var/rows_(rebuild|restore)_retained/rows=1024/width=(4|32)/(sparse|shared)/all$' \
    --warm-up-time 0.3 --measurement-time 1 --sample-size 40 \
    --nresamples 10000 --noplot --save-baseline s7c-full-2026-09-18
  ```

  Longer-run mean times (reconstruction → native, microseconds): width 4 sparse
  29.04 → 31.61, width 4 shared 32.88 → 34.42, width 32 sparse
  143.75 → 104.88, width 32 shared 116.81 → 85.97. The width-4 penalty persists
  at about 5–9%; width 32 improves about 26–27%. The 95% mean confidence intervals
  do not overlap within these four comparisons.

  Retain this implementation for S7c: it avoids per-survivor vector allocation
  and improves the measured isolated-row case (41–48%) and interleaved-row cases
  (21–42%) with retained capacity. Participant restoration improves the measured
  nonempty cases by 5–56%. Complete restoration of narrow rows remains a measured
  disadvantage. The extra saved-participant copy and temporary buffers are known
  costs, but these timings do not isolate their contribution from allocation,
  payload movement, or incidence rebuilding. Removing that extra copy or making
  incidence incremental remains separately evaluated internal optimization work;
  no further public surface is needed.

  Final diff review reconciled both methods with the approved signatures, payload
  bounds, undo provenance, and no-panic properties. Existing APIs and contracts
  are unchanged. Results, source identities, and reproduction commands are recorded
  here independently of scratch outputs. S7d is next.

- **S7d — FixedFixedBirelationSet restoration** — completed 2026-09-18; additive (green).
  Modules: src/relation/fixed_fixed.rs, src/relation/tests/fixed_fixed.rs,
  tests/property/restore.rs, and benches/restore.rs. [dep: S6c]
  Implement both methods for two fixed factors, including either or both factors
  of arity zero. Preserve factor boundaries and rebuild incidence over the union
  of both factors. Add the common relation verification and measure both methods.

  Implemented the approved restore and restore_participants methods, both returning
  () without D: Clone. Saved rows have shape (RelationId, [L1; N1], [L2; N2], D).
  No constructor, error type, visibility change, or shared test support was added.
  The settled undo contract applies: matching producer data recovers the original;
  manipulated input must not panic and has no specified restoration result.

  Row restoration extends S7b's algorithm to three columns. It appends saved rows,
  scans removed ids once for survivor destinations, and swaps both factors and
  payloads together using a temporary destination array. Local capacity/index
  guards prevent panics without revalidating the removal history. Participant
  restoration edits each typed factor in place; payloads stay in place. Both
  operations rebuild union incidence over the two factors. Column reuse avoids
  full row reconstruction, but a single restored row still incurs an all-row
  destination scan and incidence rebuild.

  Added 43 unit cases and six public-API properties. Exact cases include unequal
  arities, either/both factors empty, empty storage, interleaved/complete removal,
  unordered saved entries, coincident rows, repeated and cross-factor references,
  changed survivor payloads, identities, and non-Clone payloads. Malformed cases
  exercise counts, saved ids, and invalid references in either factor, requiring
  only freedom from panics. Structured participants preserve non-reference labels
  and expose node, edge, both, or neither reference. Independent properties use
  factors of arity two and three over up to 32 rows, with eight-id inverse-reference
  domains; separate producer roundtrips complement the independent expectations.
  Tests remain independent of other test modules.

  Verification passed: 1,708 graph-core unit cases, 53 integration cases, one
  doctest, all 21 restoration properties with PROPTEST_CASES=256, strict Clippy
  with proptest, strict rustdoc, all 1,632 benchmark fixture checks (264 fixed/fixed
  cases), formatting, and diff checks. Rust 1.87 remains reserved for S8.

  ```sh
  cargo test --locked -p umol-graph-core test_fixed_fixed_birelation_set_restore --quiet
  cargo test --locked -p umol-graph-core --quiet
  PROPTEST_CASES=256 cargo test --locked -p umol-graph-core \
    --features proptest --test property restore:: --quiet
  cargo clippy --locked -p umol-graph-core --all-targets \
    --features proptest -- -D warnings
  RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
  cargo bench --locked -p umol-graph-core --bench restore -- --test
  cargo +nightly-2026-06-04 fmt --all --check
  git diff --check
  ```

  Benchmarks use four NodeIds in the first factor and four EdgeIds in the second,
  usize payloads, 0/1/64/1,024 rows, sparse/shared references, and the seven existing
  removal patterns. Both row algorithms receive matched clone or retained-capacity
  setup; removal and input cloning remain outside timing. Participant algorithms
  start from the same compacted set. The common S6–S8 benchmark interpretation
  above applies.

  Measured on arm64 macOS 15.7.3 with rustc 1.96.0 (ac68faa20 2026-05-25), based
  on commit 72f5a09d387814d7c96f92c876af488c39a1f3c6 plus this S7d diff.
  SHA-256 identities of measured sources:

  - src/relation/fixed_fixed.rs:
    1f30d780b3d77b7f88e823e210e342f66edb5d7791624202fcd3ddff8c86fb62
  - benches/restore.rs:
    bab889e063b8098239212394970cd6f88d5c91f832d5e765327388d0d1891857

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/fixed_fixed/' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7d-2026-09-18
  ```

  All 264 cases completed. Selected short-run means below are microseconds,
  reconstruction → native restoration, at 1,024 original rows.

  | Setup | References | Restore first row | Restore interleaved rows | Restore all rows |
  | --- | --- | --- | --- | --- |
  | compacted clone | sparse | 40.08 → 39.92 | 37.89 → 39.43 | 38.37 → 36.87 |
  | compacted clone | shared | 48.67 → 52.92 | 47.92 → 49.89 | 49.75 → 47.80 |
  | retained capacity | sparse | 40.14 → 38.89 | 37.98 → 38.45 | 40.13 → 36.57 |
  | retained capacity | shared | 53.82 → 49.82 | 48.71 → 49.05 | 49.93 → 47.15 |

  Differences were small or setup-sensitive, so the first/interleaved cases were
  repeated with longer samples (16 measurements):

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- \
    'restore/fixed_fixed/(rows_(rebuild|restore)_retained|participants_(rebuild|restore))/rows=1024/width=4/(sparse|shared)/(first|interleaved)$' \
    --warm-up-time 0.3 --measurement-time 1 --sample-size 40 \
    --nresamples 10000 --noplot --save-baseline s7d-focused-2026-09-18
  ```

  | References | Removal | Row restoration, retained capacity | Participant restoration |
  | --- | --- | --- | --- |
  | sparse | first | 40.256 → 39.273 | 48.187 → 46.629 |
  | sparse | interleaved | 37.991 → 38.173 | 172.339 → 167.414 |
  | shared | first | 50.670 → 49.110 | 0.033 → 0.019 |
  | shared | interleaved | 48.126 → 49.079 | 187.542 → 183.968 |

  Shared-reference first removal leaves no surviving rows, so the 0.019 µs native
  participant result is empty-storage overhead, not reference translation. The
  other three participant cases retain rows. Sparse-first participant timing has
  overlapping 95% mean intervals (reconstruction 48.00–48.39 µs, native
  44.94–49.49 µs), so its lower native mean does not establish an improvement.

  Retain the three-column implementation: the measured retained-capacity single-row
  case improves 2–3%, while interleaved row restoration costs about 0.5–2% more
  (the sparse intervals overlap). Interleaved participant restoration improves
  about 2–3%. This is near reconstruction speed, not a broad speedup. Spare capacity
  matters: the short-run shared first-row case was about 9% slower on a compacted
  clone, and that setup does not preserve the capacity left by removal. Neither
  column reuse nor storage ownership eliminates full incidence rebuilding. These
  timings do not isolate swaps, guards, allocation, or incidence as the cause of
  the residual differences; specialized insertion or incremental incidence remains
  separately evaluated internal optimization work.

  Final API/diff review confirmed the two approved signatures, no Clone requirement,
  factor preservation, local panic guards, and no checks promising correctness for
  manipulated undo data. Existing mutation and compaction contracts are unchanged.
  Results, source identities, and commands are retained here independently of scratch.
  S7e is next.

- **S7e — FixedVarBirelationSet restoration** — completed 2026-09-19; additive (green).
  Modules: src/relation/fixed_var.rs, src/relation/tests/fixed_var.rs,
  tests/property/restore.rs, and benches/restore.rs. [dep: S6c]
  Implement both methods for the fixed column, variable buffer, offsets, and
  payloads, including zero fixed arity and empty variable factors. Add the common
  relation verification and measure both methods.

  Implemented restore with saved rows (RelationId, [L1; N1], Vec<L2>, D), and
  restore_participants with &GraphCompaction. Both return () without D: Clone.
  Matching removal data recovers the original; manipulated undo data must not panic
  and has no specified result. No constructor, error type, visibility change, or
  shared test support was added.

  Whole-row restoration extends S7c's packed-row algorithm: the temporary row slots
  hold each fixed factor, variable-buffer range, and moved payload. A linear scan
  of removed ids finds survivor destinations. The fixed, payload, and offset
  columns retain their allocations; the variable factor is gathered into one new
  packed buffer without allocating a Vec per surviving row. Local size, allocation,
  and indexing guards prevent panics without validating the undo history.
  Participant restoration edits both factors in place and leaves offsets and
  payloads untouched. Both operations rebuild union incidence.

  Whole-row restoration still visits every row and participant for one restored
  row. It temporarily holds old and new variable buffers plus fixed factors and
  payloads in row slots. Saved variable participants are copied through the old
  buffer before their final placement. This stage does not provide incremental
  incidence or allocation-free insertion.

  Added 43 unit cases and six public-API properties. Exact cases cover varying
  second-factor lengths, either/both factors empty, empty sets, interleaved and
  complete removal, unordered saved entries, changed survivor payloads, coincident
  rows, repeated/cross-factor references, identities, and non-Clone payloads.
  Structured participants exercise node, edge, both, and neither reference while
  preserving labels. Malformed cases vary counts, saved ids, and references in
  either factor; they assert only freedom from panics. Properties compare independent
  rows and incidence, inverse-reference expectations, and producer roundtrips over
  up to 32 rows with two fixed and zero to eight variable participants. Inverse
  reference domains have up to eight nodes and edges. Test modules remain independent.

  Verification passed: 1,751 graph-core unit cases, 53 integration cases, one
  doctest, all 27 restoration properties with PROPTEST_CASES=256, strict Clippy
  with proptest, strict rustdoc, all 1,984 benchmark fixture checks (528 fixed/variable
  cases), formatting, and diff checks. Rust 1.87 remains reserved for S8.

  ```sh
  cargo test --locked -p umol-graph-core test_fixed_var_birelation_set_restore --quiet
  cargo test --locked -p umol-graph-core --quiet
  PROPTEST_CASES=256 cargo test --locked -p umol-graph-core \
    --features proptest --test property restore:: --quiet
  cargo clippy --locked -p umol-graph-core --all-targets \
    --features proptest -- -D warnings
  RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
  cargo bench --locked -p umol-graph-core --bench restore -- --test
  cargo +nightly-2026-06-04 fmt --all --check
  git diff --check
  ```

  Benchmarks use four NodeIds in the fixed factor, maximum variable widths 4/32
  of EdgeIds, usize payloads, 0/1/64/1,024 rows, sparse/shared references, and the
  seven existing removal patterns. Rows congruent to six modulo eight have an
  empty variable factor; other lengths are width minus (row + 1) modulo three.
  Both row algorithms receive matched clone or retained-capacity setup; removal
  and input cloning stay outside timing. Participant algorithms start from the
  same compacted set. The common S6–S8 benchmark interpretation above applies.

  Measured on arm64 macOS 15.7.3 with rustc 1.96.0 (ac68faa20 2026-05-25), based
  on commit 181568336f18947aac38e9d17ae02da8371e0251 plus this S7e diff.
  SHA-256 identities of measured sources:

  - src/relation/fixed_var.rs:
    0ad02560e2f646b4f5af2d6702bc77f30b89b8c5b013809efe3274f7a1acaf22
  - benches/restore.rs:
    b5c14d2957c3594ceaf94c7c796fdb4379f68da1ad8f15616281983a4e080e89

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/fixed_var/' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7e-2026-09-19
  ```

  All 528 cases completed. In the short run, retained-capacity first-row restoration
  improved 24–35% and interleaved restoration improved 9–14% at 1,024 rows.
  Complete restoration was slower by 1–4% except for one width-4 shared result:
  56.41 → 78.28 µs, a 39% increase. To check that outlier and the setup-sensitive
  results, all first/interleaved/complete retained-capacity comparisons were repeated
  with longer samples (24 measurements):

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- \
    'restore/fixed_var/rows_(rebuild|restore)_retained/rows=1024/width=(4|32)/(sparse|shared)/(first|interleaved|all)$' \
    --warm-up-time 0.3 --measurement-time 1 --sample-size 40 \
    --nresamples 10000 --noplot --save-baseline s7e-focused-2026-09-19
  ```

  Longer-run means below are microseconds, reconstruction → native restoration,
  at 1,024 original rows with retained capacity. Width is the variable factor's
  maximum length; the fixed factor always has four participants.

  | Width | References | Restore first row | Restore interleaved rows | Restore all rows |
  | --- | --- | --- | --- | --- |
  | 4 | sparse | 57.83 → 38.00 | 50.37 → 43.97 | 48.82 → 50.98 |
  | 4 | shared | 65.41 → 45.75 | 58.74 → 50.98 | 55.10 → 57.10 |
  | 32 | sparse | 159.87 → 104.10 | 136.43 → 120.70 | 140.42 → 127.33 |
  | 32 | shared | 134.02 → 94.87 | 119.64 → 104.25 | 112.11 → 112.19 |

  The narrow shared complete-removal outlier did not reproduce: the longer run
  shows a 3.6% penalty, alongside 4.4% for narrow sparse rows. At width 32, the
  shared complete-removal case is effectively equal (overlapping 95% mean
  intervals). The sparse case changes from 119.26 → 121.85 µs in the short run
  to 140.42 → 127.33 µs in the longer run; this run-to-run variation does not
  support a general complete-removal speedup claim.

  Participant-reference results from the short run, in the same units:

  | Width | References | After first reference removal | After interleaved reference removal |
  | --- | --- | --- | --- |
  | 4 | sparse | 63.944 → 39.689 | 166.421 → 139.721 |
  | 4 | shared | 0.049 → 0.016 | 181.693 → 160.656 |
  | 32 | sparse | 192.780 → 145.628 | 885.070 → 822.223 |
  | 32 | shared | 0.048 → 0.015 | 919.403 → 903.037 |

  Shared first-reference removal discards every row because the fixed factor
  references node zero, even when the variable factor is empty. Those nanosecond
  results measure empty-storage overhead. Complete graph-reference removal also
  leaves no rows. The nonempty participant cases improve by about 2–38% in this run.

  Retain this implementation: the longer-run retained-capacity first-row cases
  improve 29–35% and interleaved cases 12–13%, while complete restoration of narrow
  rows remains about 4% slower. The implementation avoids per-survivor vectors,
  but copying saved variable participants twice and rebuilding incidence remain
  costs. The timings do not isolate their individual contributions. Complete-restoration optimization and incremental
  incidence remain separately evaluated internal work; no public API change is needed.

  Final diff review confirmed the approved signatures, factor boundaries, payload
  bounds, and local panic guards. Existing mutation/compaction contracts are unchanged;
  manipulated-input tests impose no result or unchanged-state guarantee. Commands,
  source identities, and decision evidence are recorded here independently of scratch.
  S7f is next.

- **S7f — VarVarBirelationSet restoration** — completed 2026-09-19; additive (green).
  Modules: src/relation/var_var.rs, src/relation/tests/var_var.rs,
  tests/property/restore.rs, and benches/restore.rs. [dep: S6c]
  Implement both methods with independent factor buffers and offsets. Cover
  unequal factor lengths and either or both factors empty. Add the common
  relation verification and measure both methods.

  Implemented restore and restore_participants with the approved unit-returning
  signatures and no D: Clone bound. Matching undo data restores the original;
  manipulated input has no specified result and must not panic. Guards cover
  local indexing, representability, and capacity hazards rather than validating
  removal history.

  Whole-row restoration scans removed ids once and places independent first/second
  buffer ranges with payloads in row slots. It rebuilds both packed buffers, reuses
  both offset columns and the payload column, and rebuilds union incidence. Neither
  surviving factor is materialized as a per-row vector. Both old and new buffers
  coexist temporarily; saved participants in both factors are copied twice.
  Participant restoration updates both buffers in place, preserving offsets and
  payloads, then rebuilds incidence. Neither method maintains incidence incrementally.

  Added 43 unit cases and six public-API properties. They cover independently varying
  factor lengths, unequal and empty factors, identity, unordered saved rows,
  interleaved/complete removal, coincident rows, changed survivor payloads,
  cross-factor references, and non-Clone payloads. Structured participants expose
  nodes, edges, both, or neither while retaining labels. Properties use independent
  row/incidence and inverse-reference oracles alongside producer roundtrips, with
  up to 32 rows and zero to eight participants independently in each factor.
  Malformed-input cases vary counts, saved ids, and references in either factor;
  they assert only freedom from panics. Test modules remain independent.

  Verification passed: 1,794 graph-core unit cases, 53 integration cases, one doctest,
  all 196 graph-core properties (including 33 restoration properties) with
  PROPTEST_CASES=256, strict Clippy with proptest, strict rustdoc, and all 2,336
  benchmark fixture checks (528 variable/variable cases), formatting, and diff checks.
  Rust 1.87 remains reserved for S8.

  ```sh
  cargo test --locked -p umol-graph-core test_var_var_birelation_set_restore --quiet
  cargo test --locked -p umol-graph-core --quiet
  PROPTEST_CASES=256 cargo test --locked -p umol-graph-core \
    --features proptest --test property --quiet
  cargo clippy --locked -p umol-graph-core --all-targets \
    --features proptest -- -D warnings
  RUSTDOCFLAGS='-D warnings' cargo doc --locked -p umol-graph-core --no-deps
  cargo bench --locked -p umol-graph-core --bench restore -- --test
  cargo +nightly-2026-06-04 fmt --all --check
  git diff --check
  ```

  Benchmarks use NodeIds in the first factor, EdgeIds in the second, usize payloads,
  0/1/64/1,024 rows, maximum widths 4/32, sparse/shared references, and the seven
  existing removal patterns. The first factor is empty when row modulo eight is
  seven, otherwise its length is width minus row modulo three. The second is empty
  when row modulo eight is six, otherwise its length is width minus (row + 1) modulo
  three. Both-empty rows are covered by tests rather than these timing fixtures.
  Row algorithms receive matched clone or retained-capacity setup, excluding
  removal and input cloning from timing. Participant algorithms start from the
  same compacted set. The common S6–S8 benchmark interpretation above applies.

  Measured on arm64 macOS 15.7.3 with rustc 1.96.0 (ac68faa20 2026-05-25), based
  on commit d63bd7c1029a6a9aacf4189ca0c5bd83002a2a8a plus this S7f diff.
  SHA-256 identities of measured sources:

  - src/relation/var_var.rs:
    f41c8cdf5403030ee75d586a642f61609f779d1bf1d899a819b53ef91ae22c03
  - benches/restore.rs:
    5b416700d639c2d6c8bd82eea5723aec100ffeffe22d856ae90a814f39765c9a

  ```sh
  cargo bench --locked -p umol-graph-core --bench restore -- 'restore/var_var/' \
    --warm-up-time 0.1 --measurement-time 0.2 --sample-size 20 \
    --nresamples 10000 --noplot --save-baseline s7f-2026-09-19
  ```

  All 528 measurements completed. Selected means below are microseconds at 1,024
  original rows, reconstruction → native restoration, with retained capacity.

  | Maximum width | References | Restore first row | Restore interleaved rows | Restore all rows |
  | --- | --- | --- | --- | --- |
  | 4 | sparse | 81.1 → 41.1 | 71.3 → 52.6 | 60.1 → 68.2 |
  | 4 | shared | 83.7 → 47.5 | 76.6 → 58.8 | 65.6 → 72.1 |
  | 32 | sparse | 270.9 → 198.7 | 275.1 → 230.0 | 243.7 → 234.4 |
  | 32 | shared | 230.8 → 167.0 | 209.9 → 183.8 | 193.1 → 198.9 |

  For participant restoration after interleaved reference removal, sparse/shared
  means were 161.5 → 117.3 / 173.6 → 136.9 µs at width 4 and
  1,533.5 → 1,451.0 / 1,602.5 → 1,593.0 µs at width 32. Shared first-reference
  removal and complete reference removal leave no rows; their timings measure
  empty-storage overhead.

  These comparisons do not establish an avoidable performance penalty or select
  between viable implementations: reconstruction does not satisfy the restoration
  contract. No longer-sample rechecks were run. Further timing requires a concrete
  decision involving a candidate with the same semantics; increasing the precision
  of this baseline comparison would not support such a decision.

  Final diff review confirmed both approved signatures, independent factor
  boundaries, non-Clone payload support, local panic guards, and unchanged existing
  mutation/compaction contracts. Results and reproduction details are retained here
  independently of scratch. S7 is complete; S8a is next.

Common relation verification, required within each of S7b–S7f:

- Exact rows and incidence after restoring interleaved ids from unordered entries;
  identity, complete removal, empty sets/factors, and changed survivor payloads.
- Freedom from panics for mismatched counts, duplicate/unexpected/missing entries,
  out-of-domain node/edge references, oversized counts, and combinations of defects.
  Do not assert a particular result or unchanged storage for manipulated input.
- Non-Clone payload cases prove the public bounds and payload preservation.
  Repeated participants and values sharing references remain representable.
- Structured participants expose nodes, edges, both, or neither; inverse transport
  preserves their other data. Incidence expectations come from independent row
  scans, including references shared within and across factors.
- Public-API properties establish removal/restoration roundtrips, invariance under
  saved-entry permutation, preservation of current survivor values, independent
  inverse-reference expectations, and freedom from panics on generated malformed
  inputs. Generate current rows independently for reference tests so roundtrips
  do not serve as the only oracle.

S7 gate: graph-core unit/integration tests and doctests, all explicit properties
with PROPTEST_CASES=256, strict graph-core Clippy/rustdoc, benchmark fixture checks,
and formatting/diff checks. Each subitem runs its focused tests, properties, and
benchmarks first; retain those measurements for closeout without repeating them
unless changes or unresolved results justify another run.

### S8 — Combined restoration laws and closeout

- **S8a — Graph/relation restoration sequences** — pending; additive (green).
  Modules: tests/property/restore.rs and benches/restore.rs.
  [dep: S7a, S7b, S7c, S7d, S7e, S7f]
  Exercise graph removal plus relation compaction, then Graph::restore,
  restore_participants on survivors, and restore of saved original relation rows.
  Cover all five relation shapes and both node/edge references. Assert original
  topology, relation rows, and independently scanned incidence; saved removed
  participants must not be translated a second time. Include sequential removals
  restored in reverse order, complete cascades, empty factors, and graph isolates.
  These joint-domain properties supplement each operation's independent tests;
  they do not use graph-IR editors or transactions. Add combined-path benchmark
  fixtures with correctness checks, including the actual separate index rebuilds
  without introducing a fused public operation. Further timing follows the
  decision requirement recorded above; a more precise comparison with the
  reconstruction baseline is not a completion requirement.

- **S8b — API, documentation, performance, and workspace gate** — pending;
  additive (green). Modules: graph-core public surface and rustdoc,
  docs/development/data-types.md, docs/development/nomenclature.md,
  discussion/166, discussion/213, and discussion/000-status.md. [dep: S8a]
  Audit the full diff against the eleven-method inventory, unit-returning
  signatures, undo provenance, panic guards, payload bounds, visibility, and
  unchanged contracts of existing methods. Review implementation directly as well
  as test results. Reconcile permanent contracts and doc 213's dependency status; keep
  editor integration explicitly outstanding. Verify public rustdoc links,
  operation laws, module documentation, and property references.
  Run the workspace gates, pinned MSRV check, and six strict graph-core rustdoc
  configurations recorded above, including explicit graph-core properties.
  Apply python-build and confirm the repository's Python 3.13 environment before
  workspace commands including umol-py; rebuild the extension for Python tests.
  Record benchmark parameters, counts, baseline/native results, and limitations
  here using maintained sources and reproducible commands. Reuse valid focused
  measurements; do not claim speedups or representative scale without evidence.
  Record verification totals and review findings here, independent of scratch.
  Complete only S6–S8; doc 166 remains open for its other operations.

The restoration dependency path is S5e → S6a → S6b → S6c → the six independent S7
subitems → S8a → S8b. All stages and both methods on every relation shape are
required. No restoration stage is optional. Later allocation/index optimizations
require separate evidence and approval; no fused restoration API is part of this
plan. Doc 213 owns the subsequent editor rewiring.

### Next steps after relation storage — updated 2026-09-19

S0–S7 are complete; S8 finishes combined restoration verification and closeout
as the prerequisite for editor restoration delegation. Then design
and implement the molecule/reaction mutation infrastructure in
[213](213-editor-overlay-storage-2026-08-27.md), then return here for operation work.
The review in 213 covers the full molecule/reaction mutation API, including
transactional and non-transactional changes, the molecule's overlay entity sets,
and delegation to relation storage, including restoration. The editor will own
the existing typed entity sets directly; no replacement storage
wrapper is required.

The following work remains in this document's scope:

1. Non-transactional editing, including the needs exposed by projections where
   transaction handling accounts for a substantial part of the implementation.
   Review those callers while designing 213 so that the common mutation
   infrastructure supports them; return here for the remaining operation work.
2. HydrogenFolder and HydrogenUnfolder using that mutation surface. Their
   operation design is essentially settled here; the remaining blocker is the
   graph-IR mutation surface and its editor-to-storage wiring.

This records scope and sequence, not an implementation plan for 213.

## Completion criteria

- The hydrogen transformer pair is public and idempotent and makes no general
  losslessness or inverse-roundtrip claim.
- Stereo ligand positions and configurations are preserved when hydrogen
  representation changes, and ambiguous implicit-hydrogen stereo input is
  rejected atomically.
- The standalone entity-field validator reports each specified literal-domain
  violation and leaves non-literal values underdetermined.
- The standalone stereo-ligand validator checks complete actual and virtual
  ligand censuses without imposing a chemistry model.
- No existing composite-validator or molecule-constraint API changes in this
  work item.
- The deferred counts-resolver correction rejects the nine-electron carbon
  reproducers with and without stereo while retaining the valid controls.
