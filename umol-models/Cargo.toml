[package]
name = "umol-models"
version = "0.1.0"
edition = "2021"

[dependencies]
umol = { path = "../umol" }
umol-macros = { path = "../umol-macros" }
umol-data = { path = "../umol-data" }
map-macro = "0.3.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
petgraph = "0.8.1"

[dev-dependencies]
rstest = "*"
